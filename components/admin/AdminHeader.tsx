import Logo from "@/components/Logo";
import { AdminNav } from "./AdminSidebar";

export default function AdminHeader({ name, email }: { name: string; email: string }) {
  const initials = name.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase() || "AD";
  return <header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-white/10 bg-[#07111F]/90 px-5 backdrop-blur-xl sm:px-8"><div className="lg:hidden"><Logo /></div><div className="hidden lg:block"><p className="text-[10px] font-bold uppercase tracking-[.18em] text-cyan-300">Admin workspace</p><p className="mt-1 text-sm font-semibold text-white">SocialRUSH operations</p></div><div className="flex items-center gap-3"><span className="hidden rounded-full bg-emerald-500/10 px-3 py-1.5 text-[10px] font-bold text-emerald-200 sm:block">● System online</span><div className="hidden text-right sm:block"><p className="text-xs font-bold text-white">{name || "Administrator"}</p><p className="mt-0.5 text-[10px] text-slate-300">{email}</p></div><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#0a1b3d] text-xs font-bold text-white">{initials}</span><details className="relative lg:hidden"><summary className="grid h-10 w-10 cursor-pointer list-none place-items-center rounded-xl border border-white/10">☰</summary><div className="absolute right-0 top-12 w-64 rounded-2xl bg-[#06142f] p-3 shadow-2xl"><AdminNav mobile/></div></details></div></header>;
}
