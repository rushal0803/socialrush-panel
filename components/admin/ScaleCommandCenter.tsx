import Link from "next/link";import type{ScaleLane}from"@/lib/analytics/scale-plan";
const tone={now:"border-orange-400/25 bg-orange-500/10 text-orange-200",next:"border-emerald-400/20 bg-emerald-500/10 text-emerald-200",hold:"border-slate-400/15 bg-white/[.03] text-slate-300"};
export default function ScaleCommandCenter({decision,lanes}:{decision:string;lanes:ScaleLane[]}){return <section className="mt-6 rounded-3xl border border-white/10 bg-[#0d0d0d] p-5 sm:p-7"><div className="flex flex-col gap-3 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.18em] text-orange-400">Day 30 · Scale Decision</p><h2 className="mt-2 text-2xl font-black text-white">60-Day Scale Command Center</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">A prioritized operating plan derived from the selected-range revenue audit. It does not manufacture forecasts, budgets or supplier economics.</p></div><div className="rounded-2xl border border-white/10 bg-black/30 px-5 py-3"><p className="text-[10px] font-black uppercase tracking-wider text-slate-500">Audit gate</p><p className="mt-1 font-black text-white">{decision}</p></div></div><div className="mt-6 grid gap-4 xl:grid-cols-2">{lanes.map((lane,i)=><article key={`${lane.title}-${i}`} className={`rounded-2xl border p-5 ${tone[lane.status]}`}><div className="flex items-start justify-between gap-3"><div><span className="text-[10px] font-black uppercase tracking-[.16em]">{lane.status==="now"?"Do now":lane.status==="next"?"Scale next":"Hold / validate"}</span><h3 className="mt-2 text-lg font-black text-white">{lane.title}</h3></div><span className="rounded-full bg-black/25 px-2.5 py-1 text-[10px] font-black">{i+1}</span></div><p className="mt-2 text-xs leading-5 text-slate-300">{lane.why}</p><ul className="mt-4 space-y-2">{lane.actions.map(a=><li key={a} className="flex gap-2 text-xs leading-5 text-slate-200"><span>•</span><span>{a}</span></li>)}</ul><Link href={lane.href} className="mt-4 inline-block text-xs font-black text-orange-200">Open workspace →</Link></article>)}</div><div className="mt-5 rounded-2xl border border-white/10 bg-black/25 p-4 text-xs leading-5 text-slate-400"><b className="text-white">Scale rule:</b> traffic is not the goal by itself. Increase effort only where verified paid revenue, conversion quality and service reliability support it; move a lane back to Hold when the audit develops a blocker.</div></section>}
