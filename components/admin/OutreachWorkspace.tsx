"use client";
import {useState,useTransition} from "react";
import {createSequence,saveSequence,sendApprovedOutreachDraft} from "@/app/admin/crm/actions";
import {getLeadContactOutreachBlockReason} from "@/lib/crm/outreach";
import type {CRMLead,CRMLeadContact,CRMLeadEnrollment,CRMOutreachMessage,CRMOutreachSequence,CRMOutreachSequenceStep,CRMOutreachSettings,CRMSuppressionEntry} from "@/lib/crm/types";
type P={settings:CRMOutreachSettings|null;sequences:CRMOutreachSequence[];steps:CRMOutreachSequenceStep[];enrollments:CRMLeadEnrollment[];messages:CRMOutreachMessage[];contacts:CRMLeadContact[];leads:CRMLead[];suppressions:CRMSuppressionEntry[]};
export default function OutreachWorkspace(p:P){const[chosen,setChosen]=useState<CRMOutreachSequence|null>(null);return <main className="mx-auto max-w-[1650px] p-4 sm:p-8"><header className="flex justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.18em] text-orange-300">Safe prospecting</p><h1 className="mt-2 text-3xl font-black text-white">Outreach</h1></div><form action={createSequence}><input type="hidden" name="name" value="New outreach sequence"/><button className="min-h-11 rounded-xl bg-orange-500 px-4 text-xs font-bold text-white">Create draft</button></form></header><div className="mt-6 rounded-xl border border-amber-400/25 bg-amber-500/[.06] p-4 text-sm text-amber-100">Automatic sending is currently disabled. Auto Send: <b>{p.settings?.auto_send?"ON":"OFF"}</b></div><section className="mt-6 space-y-3 rounded-2xl border border-white/10 bg-[#111111] p-5">{p.sequences.map(q=><div key={q.id} className="flex justify-between gap-3 rounded-xl border border-white/10 bg-[#0b0b0f] p-4"><span><b className="text-white">{q.name}</b><small className="ml-2 text-[#9CA3AF]">{p.steps.filter(x=>x.sequence_id===q.id).length} steps · {q.status}</small></span><button onClick={()=>setChosen(q)} className="text-xs font-bold text-orange-200">Edit sequence</button></div>)}</section><Queue {...p}/>{chosen&&<Builder sequence={chosen} initial={p.steps.filter(x=>x.sequence_id===chosen.id)} close={()=>setChosen(null)}/>}</main>}
function Queue({settings,messages,contacts,leads,sequences,suppressions}:P){const[pending,start]=useTransition(),[draft,setDraft]=useState<CRMOutreachMessage|null>(null),[error,setError]=useState("");const cs=new Map(contacts.map(x=>[x.id,x])),ls=new Map(leads.map(x=>[x.id,x])),ss=new Map(sequences.map(x=>[x.id,x]));const send=()=>draft&&start(async()=>{try{await sendApprovedOutreachDraft(draft.id);setDraft(null)}catch(e){setError(e instanceof Error?e.message:"Could not send outreach email.")}});return <section className="mt-6 rounded-2xl border border-white/10 bg-[#111111] p-4 sm:p-5"><p className="text-xs font-black uppercase tracking-[.18em] text-orange-300">Manual review</p><h2 className="mt-1 text-xl font-black text-white">Draft Queue</h2><p className="mt-1 text-xs text-[#9CA3AF]">Drafts are never sent automatically.</p>{messages.length?<div className="mt-4 grid gap-3 xl:grid-cols-2">{messages.map(m=>{const c=m.contact_id?cs.get(m.contact_id):undefined,l=ls.get(m.lead_id),s=m.sequence_id?ss.get(m.sequence_id):undefined,r=getLeadContactOutreachBlockReason(c,l,settings,suppressions),ok=!r&&m.provider==="resend";return <article key={m.id} className="rounded-xl border border-white/10 bg-[#0b0b0f] p-4"><div className="flex flex-wrap justify-between gap-2"><div><h3 className="font-black text-white">{l?.business_name||"Unknown business"}</h3><p className="mt-1 break-all text-xs text-[#9CA3AF]">{c?.email||"No contact email"}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-black ${r?"bg-red-500/10 text-red-200":"bg-emerald-500/10 text-emerald-200"}`}>{r?`Blocked: ${r}`:"Eligible to Send"}</span></div><p className="mt-4 text-sm font-bold text-white">{m.subject||"(No subject)"}</p><dl className="mt-3 grid grid-cols-2 gap-2 text-xs"><div><dt className="text-[#9CA3AF]">Provider</dt><dd className="mt-1 font-bold text-white">{m.provider||"—"}</dd></div><div><dt className="text-[#9CA3AF]">Status</dt><dd className="mt-1 font-bold text-white">{m.status}</dd></div><div><dt className="text-[#9CA3AF]">Sequence</dt><dd className="mt-1 font-bold text-white">{s?.name||"Standalone Draft"}</dd></div><div><dt className="text-[#9CA3AF]">Created</dt><dd className="mt-1 font-bold text-white">{new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short"}).format(new Date(m.created_at))}</dd></div></dl>{ok&&<button onClick={()=>{setError("");setDraft(m)}} className="mt-4 min-h-10 rounded-lg bg-orange-500 px-3 text-xs font-black text-white">Send Now</button>}</article>})}</div>:<div className="mt-4 rounded-xl border border-dashed border-white/15 bg-white/[.02] p-8 text-center text-sm text-[#9CA3AF]">No outreach drafts waiting.</div>}{error&&<p className="mt-3 text-sm text-red-200">{error}</p>}{draft&&<div role="dialog" aria-modal="true" aria-labelledby="send-title" className="fixed inset-0 z-50 grid place-items-center bg-black/75 p-4"><div className="w-full max-w-md rounded-2xl border border-orange-400/25 bg-[#141416] p-5"><h2 id="send-title" className="text-lg font-black text-white">Send this outreach email?</h2><dl className="mt-4 space-y-3 text-sm"><div><dt className="text-xs text-[#9CA3AF]">Recipient</dt><dd className="mt-1 break-all font-bold text-white">{cs.get(draft.contact_id||"")?.email||"Unavailable"}</dd></div><div><dt className="text-xs text-[#9CA3AF]">Subject</dt><dd className="mt-1 font-bold text-white">{draft.subject||"(No subject)"}</dd></div><div><dt className="text-xs text-[#9CA3AF]">From address</dt><dd className="mt-1 font-bold text-white">SocialRUSH &lt;growth@outreach.getsocialrush.com&gt;</dd></div></dl><div className="mt-5 grid grid-cols-2 gap-3"><button onClick={()=>setDraft(null)} className="min-h-11 rounded-xl border border-white/15 text-sm font-bold text-white">Cancel</button><button disabled={pending} onClick={send} className="min-h-11 rounded-xl bg-orange-500 text-sm font-black text-white">{pending?"Sending…":"Send Email"}</button></div></div></div>}</section>}
function Builder({sequence,initial,close}:{sequence:CRMOutreachSequence;initial:CRMOutreachSequenceStep[];close:()=>void}){const[pending,start]=useTransition(),[name,setName]=useState(sequence.name);return <div className="fixed inset-0 z-50 grid place-items-center bg-black/75 p-4"><div className="w-full max-w-md rounded-2xl bg-[#141416] p-5"><h2 className="text-xl font-black text-white">Sequence builder</h2><label className="mt-4 block text-xs text-white">Name<input value={name} onChange={e=>setName(e.target.value)} className="mt-1 h-10 w-full rounded-lg bg-[#0b0b0f] px-3 text-white"/></label><p className="mt-3 text-xs text-[#9CA3AF]">{initial.length} existing steps are preserved.</p><div className="mt-5 grid grid-cols-2 gap-3"><button onClick={close} className="min-h-11 rounded-xl border border-white/15 text-white">Close</button><button disabled={pending} onClick={()=>start(async()=>{await saveSequence({id:sequence.id,name,description:sequence.description||"",status:sequence.status,dailySendLimit:sequence.daily_send_limit||20,timezone:sequence.timezone||"Asia/Kolkata",sendWindowStart:sequence.send_window_start||"09:00",sendWindowEnd:sequence.send_window_end||"17:00",steps:initial.map(x=>({id:x.id,delayDays:x.delay_days,subject:x.subject_template,body:x.body_template,stopOnReply:x.stop_on_reply}))});close()})} className="min-h-11 rounded-xl bg-orange-500 font-black text-white">Save Sequence</button></div></div></div>}
