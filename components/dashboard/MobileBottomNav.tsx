"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Home, PlusCircle, ReceiptText, WalletCards, UserRound } from "lucide-react";
const links = [{ href: "/dashboard", label: "Home", Icon: Home }, { href: "/dashboard/new-order", label: "New Order", Icon: PlusCircle }, { href: "/dashboard/orders", label: "Orders", Icon: ReceiptText }, { href: "/dashboard/wallet", label: "Wallet", Icon: WalletCards }, { href: "/dashboard/account", label: "Account", Icon: UserRound }];
export default function MobileBottomNav() { const pathname = usePathname(); return <nav className="fixed inset-x-0 bottom-0 z-[65] border-t border-white/10 bg-[#0c0e14]/95 px-1.5 pb-[env(safe-area-inset-bottom)] pt-1.5 shadow-[0_-12px_30px_-24px_rgba(0,0,0,.95)] backdrop-blur lg:hidden" aria-label="Primary dashboard navigation"><div className="mx-auto grid max-w-lg grid-cols-5">{links.map(({ href, label, Icon }) => { const active = href === "/dashboard" ? pathname === href : pathname.startsWith(href); return <Link key={href} href={href} aria-current={active ? "page" : undefined} className={`flex min-h-[3.25rem] min-w-0 flex-col items-center justify-center gap-0.5 rounded-xl px-0.5 text-[9px] font-bold leading-tight transition-colors min-[360px]:min-h-14 min-[360px]:gap-1 min-[360px]:text-[10px] ${active ? "bg-orange-400/[.11] text-orange-200" : "text-slate-400 hover:bg-white/[.04] hover:text-slate-200"}`}><Icon className="h-[1.15rem] w-[1.15rem] shrink-0 min-[360px]:h-5 min-[360px]:w-5" aria-hidden="true"/><span className="max-w-full whitespace-nowrap">{label}</span></Link>; })}</div></nav>; }
