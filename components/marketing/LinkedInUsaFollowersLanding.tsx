"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowRight, Check, ChevronDown, Globe2, Link2, LockKeyhole, Minus, Plus, ShieldCheck, Users, WalletCards } from "lucide-react";
import { FaLinkedinIn as LinkedIn } from "react-icons/fa6";
import PublicShell from "@/components/marketing/PublicShell";
import { getServiceById } from "@/lib/smm-service-catalog";
import { calculateServiceTotal } from "@/lib/service-pricing";
import { formatCurrency } from "@/lib/currency";
import { usePreferredCurrency } from "@/lib/currency/use-currency";
import { linkRules, validateCampaignLink } from "@/lib/order-service-experience";
import { linkedInUsaFollowersFaqs as faqs } from "@/lib/linkedin-usa-followers-content";

const service = getServiceById("linkedin-usa-followers");
const number = (value: number) => value >= 1000 ? `${value / 1000}K` : value.toLocaleString("en-IN");

const service = getServiceById("linkedin-usa-followers");
const number = (value: number) => value >= 1000 ? `${value / 1000}K` : value.toLocaleString("en-IN");
const faqs = [
  ["What are LinkedIn USA Followers?", "This is SocialRUSH's USA-targeted LinkedIn USA Followers service for an eligible public personal profile. The current catalog rate, delivery estimate and order details are shown before you continue."],
  ["How do I buy LinkedIn USA Followers?", "Choose a valid quantity, provide the required public LinkedIn personal profile URL, review the live total, and continue through the secure SocialRUSH order flow."],
  ["Which LinkedIn URL should I provide?", "Provide a public personal profile URL in the linkedin.com/in/username format. Company, post and unrelated URLs are not eligible for this service."],
  ["Do I need to share my LinkedIn password?", "No. SocialRUSH only needs the required public profile link. Never share a LinkedIn password, email password, OTP or recovery code."],
  ["How much do LinkedIn USA Followers cost?", "The order builder shows the current catalog rate per 1,000 and calculates the total for your selected quantity before secure checkout."],
  ["What is the minimum and maximum order?", "The live service limits are shown in the order builder before you continue, so your chosen quantity stays within the current catalog range."],
  ["How long does delivery take?", "The current catalog delivery estimate is displayed in the order summary before checkout. Delivery timing can depend on the service and submitted public profile."],
  ["Is refill support included?", "The current catalog refill/support policy is displayed in the order summary and applies to this service as shown when you order."],
  ["Can I track my order?", "Yes. After checkout, order details and progress are available through the SocialRUSH dashboard."],
  ["What is the difference between USA Followers and USA Connections?", "Followers are people following a profile to receive its updates. Connections are professional network relationships. This service is for follower-focused ordering on eligible personal profiles; USA Connections is a separate service."],
  ["How are USA Followers different from general LinkedIn Followers?", "This USA Followers service is a distinct catalog option with USA-targeted service positioning and a public personal-profile requirement. The general LinkedIn Followers page covers the broader follower service, including profile or company-page use where supported."],
  ["Do followers guarantee engagement, leads or sales?", "No. SocialRUSH does not guarantee engagement, leads, jobs, clients, sales, revenue, reach or other commercial outcomes."],
] as const;

export { faqs as linkedInUsaFollowersFaqs };

export default function LinkedInUsaFollowersLanding() {
  const { currency, rates } = usePreferredCurrency("INR");
  const [quantity, setQuantity] = useState(service?.minQuantity ?? 100);
  const [link, setLink] = useState("");
  const [attempted, setAttempted] = useState(false);
  if (!service) return null;
  const rule = linkRules[service.code];
  const linkError = link.trim() ? validateCampaignLink(link, rule) : "";
  const quantityValid = Number.isInteger(quantity) && quantity >= service.minQuantity && quantity <= service.maxQuantity;
  const valid = quantityValid && Boolean(link.trim()) && !linkError;
  const updateQuantity = (value: number) => setQuantity(Math.max(service.minQuantity, Math.min(service.maxQuantity, Math.round(value))));
  const presets = [100, 500, 1000, 2500, 5000, 10000].filter(value => value >= service.minQuantity && value <= service.maxQuantity);
  const href = `/dashboard/new-order?platform=linkedin&service=${service.code}&resume=1&quantity=${quantity}&link=${encodeURIComponent(link.trim())}&currency=${currency}`;
  const price = formatCurrency(service.pricePer1000, currency, rates);
  const total = formatCurrency(calculateServiceTotal(service.code, quantity), currency, rates);
  return <PublicShell tone="light3d"><main className="overflow-hidden bg-[#090d14] text-white">
    <section className="relative border-b border-white/10 px-4 pb-14 pt-7 sm:px-6 lg:px-8 lg:pb-20"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_83%_19%,rgba(10,102,194,.28),transparent_26%),radial-gradient(circle_at_10%_8%,rgba(255,151,34,.18),transparent_25%)]" />
      <div className="relative mx-auto max-w-7xl"><nav aria-label="Breadcrumb" className="mb-8 flex gap-2 text-xs font-bold text-slate-400"><Link href="/">Home</Link><span>/</span><Link href="/services">Services</Link><span>/</span><span className="text-slate-200">LinkedIn USA Followers</span></nav><div className="grid items-center gap-9 lg:grid-cols-[.95fr_1.05fr]">
        <div><p className="inline-flex items-center gap-2 rounded-full border border-[#0a66c2]/45 bg-[#0a66c2]/10 px-3 py-2 text-[10px] font-black tracking-[.16em] text-sky-100"><LinkedIn className="h-4 w-4" />LINKEDIN USA FOLLOWERS</p><h1 className="mt-5 max-w-3xl text-4xl font-black leading-[1.04] tracking-[-.055em] sm:text-5xl lg:text-6xl">Buy LinkedIn <span className="bg-gradient-to-r from-orange-300 to-amber-200 bg-clip-text text-transparent">USA Followers</span></h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-300 sm:text-lg">Choose a USA-focused LinkedIn follower service with transparent live pricing, public-link ordering, secure checkout and dashboard tracking.</p><div className="mt-7 flex flex-col gap-3 min-[420px]:flex-row"><a href="#order" className="inline-flex min-h-12 items-center justify-center rounded-xl bg-gradient-to-r from-[#ff7a00] to-[#ffb000] px-6 text-sm font-black shadow-[0_15px_32px_-12px_rgba(255,147,0,.8)]">Start Your Order</a><Link href="/services" className="inline-flex min-h-12 items-center justify-center rounded-xl border border-white/15 bg-white/[.05] px-6 text-sm font-black">Explore LinkedIn Services</Link></div><div className="mt-6 flex flex-wrap gap-x-5 gap-y-2 text-xs font-bold text-slate-300">{["No password required", "Public LinkedIn link", "Live pricing", "Secure checkout", "Order tracking"].map(item => <span key={item} className="flex items-center gap-1.5"><Check className="h-4 w-4 text-orange-300" />{item}</span>)}</div></div><AudiencePreview price={price} />
       </div></div>
    </section>
    <section id="order" className="scroll-mt-24 border-b border-white/10 bg-[#0d131e] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-[minmax(0,1fr)_390px]"><article className="rounded-[2rem] border border-[#0a66c2]/30 bg-[radial-gradient(circle_at_95%_4%,rgba(10,102,194,.18),transparent_26%),linear-gradient(145deg,#182433,#101116)] p-6 sm:p-8"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">Live order builder</p><h2 className="mt-3 text-2xl font-black sm:text-3xl">Build Your LinkedIn USA Followers Order</h2><p className="mt-2 text-sm text-slate-300">Use a valid quantity and the required public LinkedIn personal-profile link.</p><div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-3">{presets.map(value => <button type="button" key={value} onClick={() => updateQuantity(value)} className={`min-h-[72px] rounded-2xl border p-3 text-left transition focus:outline-none focus:ring-2 focus:ring-orange-300 ${quantity === value ? "border-[#0a66c2] bg-[#0a66c2]/15" : "border-white/10 bg-white/[.035] hover:border-white/25"}`}><b>{number(value)}</b><span className="mt-1 block text-[10px] font-bold uppercase text-sky-200">Followers</span></button>)}</div><label className="mt-7 block text-sm font-black">Exact quantity<span className="mt-2 flex overflow-hidden rounded-2xl border border-white/10 bg-black/25 focus-within:border-[#0a66c2]"><button type="button" aria-label="Decrease quantity" onClick={() => updateQuantity(quantity - 1)} className="grid min-h-14 w-14 place-items-center text-orange-300"><Minus className="h-4 w-4" /></button><input aria-label="Follower quantity" inputMode="numeric" value={quantity || ""} onChange={event => setQuantity(event.target.value ? Number(event.target.value.replace(/\D/g, "")) : 0)} className="min-w-0 flex-1 bg-transparent text-center outline-none"/><button type="button" aria-label="Increase quantity" onClick={() => updateQuantity(quantity + 1)} className="grid min-h-14 w-14 place-items-center text-orange-300"><Plus className="h-4 w-4" /></button></span></label><p className={`mt-2 text-xs ${attempted && !quantityValid ? "font-bold text-red-300" : "text-slate-400"}`}>{attempted && !quantityValid ? `Enter a whole quantity from ${number(service.minQuantity)} to ${number(service.maxQuantity)}.` : `Minimum ${number(service.minQuantity)} · Maximum ${number(service.maxQuantity)}`}</p><label className="mt-7 block text-sm font-black">{rule.label}<span className={`mt-2 flex rounded-2xl border bg-black/25 focus-within:border-[#0a66c2] ${link.trim() && !linkError ? "border-emerald-400/50" : attempted && (!link.trim() || linkError) ? "border-red-400/60" : "border-white/10"}`}><LinkedIn className="m-4 h-5 w-5 shrink-0 text-[#0a66c2]"/><input value={link} onChange={event => setLink(event.target.value)} placeholder={rule.placeholder} className="min-w-0 flex-1 bg-transparent pr-4 outline-none" /></span></label><p className={`mt-2 text-xs leading-6 ${attempted && (!link.trim() || linkError) ? "font-bold text-red-300" : "text-slate-400"}`}>{link.trim() && !linkError ? "Valid public LinkedIn personal profile URL" : attempted && (!link.trim() || linkError) ? linkError || "Enter a public LinkedIn personal profile URL." : rule.helper}</p></article><OrderSummary quantity={quantity} linkReady={Boolean(link.trim()) && !linkError} price={price} total={total} valid={valid} href={href} attempted={attempted} setAttempted={setAttempted} /></div></section>
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-8 lg:grid-cols-[.9fr_1.1fr] lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">USA professional audience · illustrative overview</p><h2 className="mt-3 text-3xl font-black">Build a More Established Professional Audience</h2><p className="mt-4 max-w-xl text-sm leading-7 text-slate-300">Founders, consultants, professionals, recruiters, B2B brands, agencies and personal brands may choose a clear follower-focused profile workflow when the USA-targeted catalog option fits their campaign. It does not promise leads, sales, jobs, clients or engagement.</p><div className="mt-6 space-y-4">{[["Public profile first", "Keep the eligible profile accessible while an order is processing."], ["Transparent details", "Review the current rate, quantity limits, delivery estimate and refill policy before checkout."], ["Track the workflow", "Use the SocialRUSH dashboard to keep order status organized."]].map(([title, body]) => <div key={title} className="flex gap-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-orange-300"/><div><h3 className="font-black">{title}</h3><p className="mt-1 text-sm text-slate-300">{body}</p></div></div>)}</div></div><ProfessionalOverview /></div></section>
    <section className="border-y border-white/10 bg-[#0d131e] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">How it works</p><h2 className="mt-3 text-3xl font-black">A clear, secure order journey</h2><ol className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{["Choose LinkedIn USA Followers", "Select quantity", "Paste public LinkedIn URL", "Review live price", "Continue to secure checkout", "Track your order"].map((step, index) => <li key={step} className="relative rounded-2xl border border-white/10 bg-white/[.035] p-5 lg:after:absolute lg:after:right-[-1rem] lg:after:top-1/2 lg:after:h-px lg:after:w-4 lg:after:bg-orange-300/40 lg:last:after:hidden"><span className="text-xs font-black text-orange-300">0{index + 1}</span><p className="mt-3 text-sm font-black">{step}</p></li>)}</ol></div></section>
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Trust and safety</p><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[[LockKeyhole,"Public-Link Ordering","No LinkedIn login details are required."],[WalletCards,"Transparent Pricing","The builder calculates the current catalog total."],[ShieldCheck,"Secure Checkout","Continue into the existing SocialRUSH order flow."],[Globe2,"USA-Focused Option","A distinct USA-targeted catalog service."],[Users,"Dashboard Tracking","Review order status after checkout."],[Link2,"Refill / Support","Current catalog terms are visible before ordering."]].map(([Icon,title,text]) => { const I = Icon as typeof LockKeyhole; return <article key={String(title)} className="rounded-2xl border border-[#0a66c2]/25 bg-[#0a66c2]/[.06] p-5"><I className="h-5 w-5 text-orange-300"/><h3 className="mt-4 font-black">{String(title)}</h3><p className="mt-2 text-sm leading-6 text-slate-300">{String(text)}</p></article>; })}</div><p className="mt-8 text-center text-sm text-slate-400">SocialRUSH never asks for your LinkedIn password, OTP, email credentials or recovery code.</p></div></section>
    <section className="border-y border-white/10 bg-[#0d131e] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-6xl gap-6 lg:grid-cols-2"><article><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Followers vs connections</p><h2 className="mt-3 text-3xl font-black">Choose the LinkedIn action that fits your goal</h2><p className="mt-4 text-sm leading-7 text-slate-300">This service owns follower intent for eligible personal profiles. Followers typically receive profile updates; connections represent professional network relationships on personal profiles. Choose USA Connections when you specifically need the connection service.</p><div className="mt-6 flex flex-wrap gap-3"><Link href="/services/linkedin-usa-connections" className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-[#0a66c2]/40 bg-[#0a66c2]/10 px-4 text-sm font-black text-sky-100">Explore USA Connections <ArrowRight className="h-4 w-4" /></Link><Link href="/linkedin-followers" className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/15 px-4 text-sm font-black">General LinkedIn Followers <ArrowRight className="h-4 w-4" /></Link></div></article><div className="rounded-[2rem] border border-white/10 bg-white/[.035] p-6"><div className="border-b border-white/10 pb-4"><p className="font-black">LinkedIn USA Followers</p><p className="mt-2 text-sm text-slate-300">Follower-focused ordering for an eligible public personal profile with the USA-targeted catalog option.</p></div><div className="border-b border-white/10 py-4"><p className="font-black">LinkedIn USA Connections</p><p className="mt-2 text-sm text-slate-300">Connection-focused ordering for a public personal profile.</p></div><div className="pt-4"><p className="font-black">General LinkedIn Followers</p><p className="mt-2 text-sm text-slate-300">A broader follower service that supports a public profile or company page where applicable.</p></div></div></div></section>
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><p className="text-center text-xs font-black uppercase tracking-[.16em] text-orange-300">Service FAQ</p><h2 className="mt-3 text-center text-3xl font-black">LinkedIn USA Followers questions</h2><div className="mt-8 space-y-3">{faqs.map(([question, answer], index) => <details key={question} open={index === 0} className="group rounded-2xl border border-white/10 bg-white/[.035] p-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 text-sm font-black">{question}<ChevronDown className="h-5 w-5 text-orange-300 transition group-open:rotate-180"/></summary><p className="mt-4 border-t border-white/10 pt-4 text-sm leading-7 text-slate-300">{answer}</p></details>)}</div><div className="mt-10 flex flex-wrap justify-center gap-3">{[["Services","/services"],["Pricing","/pricing"],["LinkedIn packages","/packages?platform=linkedin"],["LinkedIn growth hub","/linkedin-growth-india"],["LinkedIn guides","/blog"]].map(([label, href]) => <Link key={href} href={href} className="rounded-xl border border-white/15 px-5 py-3 text-sm font-black">{label}</Link>)}</div></div></section>
  </main></PublicShell>;
}

function OrderSummary({ quantity, linkReady, price, total, valid, href, attempted, setAttempted }: { quantity: number; linkReady: boolean; price: string; total: string; valid: boolean; href: string; attempted: boolean; setAttempted: (value: boolean) => void }) { if (!service) return null; return <aside className="rounded-[2rem] border border-orange-400/25 bg-[linear-gradient(160deg,#21150e,#101827_50%,#0b0c10)] p-6 lg:sticky lg:top-28 lg:h-fit"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">Order summary</p><h3 className="mt-2 text-xl font-black">LinkedIn USA Followers</h3><dl className="mt-6 space-y-3 text-sm">{[["Service selected",service.name],["Quantity",number(quantity)],["Live rate",`${price} / 1K`],["Delivery",service.deliveryTime],["Refill/support",service.refillPolicy],["URL status",linkReady ? "Ready" : "Required"]].map(([label, value]) => <div key={label} className="flex justify-between gap-4"><dt className="text-slate-400">{label}</dt><dd className="text-right font-black">{value}</dd></div>)}</dl><div className="mt-6 rounded-2xl border border-orange-400/20 bg-black/25 p-4"><p className="text-[10px] font-black uppercase text-orange-200">Calculated total</p><p className="mt-2 text-3xl font-black">{total}</p></div><Link href={valid ? href : "#order"} onClick={event => { if (!valid) { event.preventDefault(); setAttempted(true); } }} aria-disabled={!valid} className={`mt-6 inline-flex min-h-14 w-full items-center justify-center gap-2 rounded-2xl text-sm font-black ${valid ? "bg-gradient-to-r from-[#ff7a00] to-[#ffb000]" : "bg-white/10 text-slate-400"}`}>Continue to Secure Order <ArrowRight className="h-4 w-4" /></Link>{attempted && !valid ? <p className="mt-3 text-center text-xs text-red-300">Check your quantity and public profile URL to continue.</p> : <p className="mt-3 text-center text-[11px] text-slate-400">Secure checkout · No password required</p>}</aside>; }

function AudiencePreview({ price }: { price: string }) { if (!service) return null; return <aside aria-label="Illustrative USA professional audience interface preview" className="relative overflow-hidden rounded-[2rem] border border-[#0a66c2]/35 bg-[linear-gradient(145deg,#111d2d,#0d1016_52%,#1a130c)] p-5 shadow-[0_32px_80px_-40px_rgba(10,102,194,.8)] sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-xl bg-[#0a66c2]"><LinkedIn className="h-6 w-6"/></span><div><p className="font-black">USA Audience Growth</p><p className="text-[10px] font-bold text-slate-400">Professional followers · UI preview</p></div></div><span className="rounded-full bg-[#0a66c2]/15 px-2.5 py-1 text-[9px] font-black text-sky-200">UI PREVIEW</span></div><div className="mt-5 rounded-2xl border border-white/10 bg-[#090e17] p-4"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-full bg-gradient-to-br from-sky-200 to-[#0a66c2] font-black text-slate-950">SR</span><div><p className="font-black">Professional profile</p><p className="text-xs text-slate-400">United States audience indicator</p></div><Globe2 className="ml-auto h-7 w-7 text-orange-300"/></div><div className="mt-5 grid grid-cols-3 gap-2">{[["USA Audience","Focused"],["Professional","Followers"],["Network","Activity"]].map(([label,value]) => <div key={label} className="rounded-xl border border-white/[.08] bg-white/[.035] p-3"><p className="text-[9px] text-slate-400">{label}</p><p className="mt-1 text-xs font-black">{value}</p></div>)}</div><div className="mt-5 flex h-16 items-end gap-2 border-b border-white/10 pb-1">{[28,42,34,57,45,68,59].map((height,index) => <span key={index} className="flex-1 rounded-t bg-gradient-to-t from-[#0a66c2] to-sky-300/80" style={{ height: `${height}%` }} />)}</div><p className="mt-2 text-[10px] text-slate-500">Illustrative follower activity — not customer analytics or a guaranteed result.</p></div><div className="mt-4 grid grid-cols-2 gap-2 text-xs">{[["Starting rate",`${price} / 1K`],["Delivery",service.deliveryTime],["Destination","Personal profile"],["Refill support",service.refillPolicy]].map(([label,value]) => <div key={label} className="rounded-xl border border-white/[.08] bg-white/[.035] p-3"><p className="text-slate-400">{label}</p><p className="mt-1 font-black">{value}</p></div>)}</div></aside>; }

function ProfessionalOverview() { return <div aria-label="Illustrative professional audience pathway" className="rounded-[2rem] border border-[#0a66c2]/25 bg-[#f6f8fb] p-5 text-slate-900 shadow-[0_28px_70px_-44px_rgba(10,102,194,.9)]"><div className="flex items-center justify-between border-b border-slate-200 pb-4"><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[#0a66c2] text-white"><LinkedIn className="h-5 w-5"/></span><div><p className="text-sm font-black">Professional Audience Overview</p><p className="text-[10px] font-bold text-slate-500">Illustrative service workflow</p></div></div><span className="rounded-full bg-orange-100 px-2 py-1 text-[9px] font-black text-orange-700">USA</span></div><div className="mt-5 space-y-3">{[["Profile / Company","Public professional presence"],["Followers","Follower-focused service selection"],["Content Presence","Clear public profile context"],["Professional Visibility","Illustrative opportunity — no outcome promise"]].map(([title,text], index) => <div key={title} className="flex items-center gap-3"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-[#0a66c2]/10 text-xs font-black text-[#0a66c2]">0{index + 1}</span><div className="min-w-0 flex-1 rounded-xl border border-slate-200 bg-white p-3"><p className="text-sm font-black">{title}</p><p className="mt-1 text-xs text-slate-500">{text}</p></div></div>)}</div></div>; }
