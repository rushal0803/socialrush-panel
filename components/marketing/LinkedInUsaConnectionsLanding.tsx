"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowRight, Check, ChevronDown, Globe2, Link2, LockKeyhole, Minus, Plus, ShieldCheck, Users, WalletCards } from "lucide-react";
import { FaLinkedinIn as LinkedIn } from "react-icons/fa6";
import PublicShell from "@/components/marketing/PublicShell";
import { getServiceById } from "@/lib/smm-service-catalog";
import { calculateServiceTotal } from "@/lib/service-pricing";
import { formatCurrency } from "@/lib/currency";
import { usePreferredCurrency } from "@/lib/currency/use-currency";
import { linkRules, validateCampaignLink } from "@/lib/order-service-experience";

const service = getServiceById("linkedin-usa-connections");
const number = (value: number) => value >= 1000 ? `${value / 1000}K` : value.toLocaleString("en-IN");
const faqs = [
  ["What are LinkedIn USA Connections?", "This is SocialRUSH’s USA-targeted LinkedIn Connections service for an eligible public personal profile. The catalog details, rate and delivery estimate are shown before you continue."],
  ["Which LinkedIn URL should I provide?", "Provide the public personal profile URL in the linkedin.com/in/username format. Company, post and unrelated URLs are not eligible for this service."],
  ["Do I need to provide my LinkedIn password?", "No. SocialRUSH only needs the required public profile link. Never share a LinkedIn password, email password, OTP or recovery code."],
  ["How much do LinkedIn USA Connections cost?", "The order builder displays the current catalog rate per 1,000 and calculates the total from your selected quantity before secure checkout."],
  ["Can I track my order?", "Yes. After checkout, order details and progress are available through the SocialRUSH dashboard."],
  ["Are Connections the same as Followers?", "No. Connections relate to a professional network relationship, while Followers are people who follow profile or company updates. See the LinkedIn Followers service for follower-focused ordering."],
  ["Does this service guarantee leads, jobs or sales?", "No. SocialRUSH does not guarantee commercial outcomes, reach, engagement, hiring outcomes, leads or sales."],
] as const;

export default function LinkedInUsaConnectionsLanding() {
  const { currency, rates } = usePreferredCurrency("INR");
  const [quantity, setQuantity] = useState(service?.minQuantity ?? 100);
  const [link, setLink] = useState("");
  const [attempted, setAttempted] = useState(false);
  if (!service) return null;
  const rule = linkRules[service.code];
  const linkError = link.trim() ? validateCampaignLink(link, rule) : "";
  const quantityValid = Number.isInteger(quantity) && quantity >= service.minQuantity && quantity <= service.maxQuantity;
  const valid = quantityValid && Boolean(link.trim()) && !linkError;
  const updateQuantity = (value: number) => setQuantity(Math.max(service.minQuantity, Math.min(service.maxQuantity, Math.round(value))));
  const presets = [100, 500, 1000, 2500, 5000, 10000].filter(value => value >= service.minQuantity && value <= service.maxQuantity);
  const href = `/dashboard/new-order?platform=linkedin&service=${service.code}&resume=1&quantity=${quantity}&link=${encodeURIComponent(link.trim())}`;
  const price = formatCurrency(service.pricePer1000, currency, rates);
  const total = formatCurrency(calculateServiceTotal(service.code, quantity), currency, rates);
  return <PublicShell tone="light3d"><main className="overflow-hidden bg-[#090d14] text-white">
    <section className="relative border-b border-white/10 px-4 pb-14 pt-7 sm:px-6 lg:px-8 lg:pb-20">
      <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_83%_19%,rgba(10,102,194,.28),transparent_26%),radial-gradient(circle_at_10%_8%,rgba(255,151,34,.18),transparent_25%)]" />
      <div className="relative mx-auto max-w-7xl"><nav aria-label="Breadcrumb" className="mb-8 flex gap-2 text-xs font-bold text-slate-400"><Link href="/">Home</Link><span>/</span><Link href="/services">Services</Link><span>/</span><span className="text-slate-200">LinkedIn USA Connections</span></nav><div className="grid items-center gap-9 lg:grid-cols-[.95fr_1.05fr]">
        <div><p className="inline-flex items-center gap-2 rounded-full border border-[#0a66c2]/45 bg-[#0a66c2]/10 px-3 py-2 text-[10px] font-black tracking-[.16em] text-sky-100"><LinkedIn className="h-4 w-4" />LINKEDIN USA CONNECTIONS</p><h1 className="mt-5 max-w-3xl text-4xl font-black leading-[1.04] tracking-[-.055em] sm:text-5xl lg:text-6xl">Buy LinkedIn <span className="bg-gradient-to-r from-orange-300 to-amber-200 bg-clip-text text-transparent">USA Connections</span></h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-300 sm:text-lg">Select a quantity, add your required public LinkedIn profile URL, review live service details, then continue securely and track the order with SocialRUSH.</p><div className="mt-7 flex flex-col gap-3 min-[420px]:flex-row"><a href="#order" className="inline-flex min-h-12 items-center justify-center rounded-xl bg-gradient-to-r from-[#ff7a00] to-[#ffb000] px-6 text-sm font-black shadow-[0_15px_32px_-12px_rgba(255,147,0,.8)]">Start Your Order</a><Link href="/services" className="inline-flex min-h-12 items-center justify-center rounded-xl border border-white/15 bg-white/[.05] px-6 text-sm font-black">View LinkedIn Services</Link></div><div className="mt-6 flex flex-wrap gap-x-5 gap-y-2 text-xs font-bold text-slate-300">{["USA-focused service", "Public-link ordering", "No password required", "Live pricing", "Secure checkout", "Dashboard tracking"].map(item => <span key={item} className="flex items-center gap-1.5"><Check className="h-4 w-4 text-orange-300" />{item}</span>)}</div></div>
        <NetworkPreview price={price} />
      </div></div>
    </section>
    <section id="order" className="scroll-mt-24 border-b border-white/10 bg-[#0d131e] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-[minmax(0,1fr)_390px]"><article className="rounded-[2rem] border border-[#0a66c2]/30 bg-[radial-gradient(circle_at_95%_4%,rgba(10,102,194,.18),transparent_26%),linear-gradient(145deg,#182433,#101116)] p-6 sm:p-8"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">Live order builder</p><h2 className="mt-3 text-2xl font-black sm:text-3xl">Build Your LinkedIn USA Connections Order</h2><p className="mt-2 text-sm text-slate-300">Use valid quantity limits and the required public personal profile link.</p><div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-3">{presets.map(value => <button type="button" key={value} onClick={() => updateQuantity(value)} className={`min-h-[72px] rounded-2xl border p-3 text-left ${quantity === value ? "border-[#0a66c2] bg-[#0a66c2]/15" : "border-white/10 bg-white/[.035]"}`}><b>{number(value)}</b><span className="mt-1 block text-[10px] font-bold uppercase text-sky-200">Connections</span></button>)}</div><label className="mt-7 block text-sm font-black">Quantity<span className="mt-2 flex overflow-hidden rounded-2xl border border-white/10 bg-black/25 focus-within:border-[#0a66c2]"><button type="button" aria-label="Decrease quantity" onClick={() => updateQuantity(quantity - 1)} className="grid min-h-14 w-14 place-items-center text-orange-300"><Minus className="h-4 w-4" /></button><input aria-label="Connection quantity" inputMode="numeric" value={quantity || ""} onChange={event => setQuantity(event.target.value ? Number(event.target.value.replace(/\D/g, "")) : 0)} className="min-w-0 flex-1 bg-transparent text-center outline-none"/><button type="button" aria-label="Increase quantity" onClick={() => updateQuantity(quantity + 1)} className="grid min-h-14 w-14 place-items-center text-orange-300"><Plus className="h-4 w-4" /></button></span></label><p className={`mt-2 text-xs ${attempted && !quantityValid ? "font-bold text-red-300" : "text-slate-400"}`}>{attempted && !quantityValid ? `Enter a whole quantity from ${number(service.minQuantity)} to ${number(service.maxQuantity)}.` : `Minimum ${number(service.minQuantity)} · Maximum ${number(service.maxQuantity)}`}</p><label className="mt-7 block text-sm font-black">{rule.label}<span className={`mt-2 flex rounded-2xl border bg-black/25 focus-within:border-[#0a66c2] ${link.trim() && !linkError ? "border-emerald-400/50" : attempted && (!link.trim() || linkError) ? "border-red-400/60" : "border-white/10"}`}><LinkedIn className="m-4 h-5 w-5 shrink-0 text-[#0a66c2]"/><input value={link} onChange={event => setLink(event.target.value)} placeholder={rule.placeholder} className="min-w-0 flex-1 bg-transparent pr-4 outline-none" /></span></label><p className={`mt-2 text-xs leading-6 ${attempted && (!link.trim() || linkError) ? "font-bold text-red-300" : "text-slate-400"}`}>{link.trim() && !linkError ? "✓ Valid public LinkedIn personal profile URL" : attempted && (!link.trim() || linkError) ? linkError || "Enter a public LinkedIn personal profile URL." : rule.helper}</p></article><aside className="rounded-[2rem] border border-orange-400/25 bg-[linear-gradient(160deg,#21150e,#101827_50%,#0b0c10)] p-6 lg:sticky lg:top-28 lg:h-fit"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">Order summary</p><h3 className="mt-2 text-xl font-black">LinkedIn USA Connections</h3><dl className="mt-6 space-y-3 text-sm">{[["Quantity", number(quantity)], ["Live rate", `${price} / 1K`], ["Delivery", service.deliveryTime], ["Refill/support", service.refillPolicy], ["Destination", link.trim() && !linkError ? "Ready" : "Required"]].map(([label, value]) => <div key={label} className="flex justify-between gap-4"><dt className="text-slate-400">{label}</dt><dd className="text-right font-black">{value}</dd></div>)}</dl><div className="mt-6 rounded-2xl border border-orange-400/20 bg-black/25 p-4"><p className="text-[10px] font-black uppercase text-orange-200">Calculated total</p><p className="mt-2 text-3xl font-black">{total}</p></div><Link href={valid ? href : "#order"} onClick={event => { if (!valid) { event.preventDefault(); setAttempted(true); } }} aria-disabled={!valid} className={`mt-6 inline-flex min-h-14 w-full items-center justify-center gap-2 rounded-2xl text-sm font-black ${valid ? "bg-gradient-to-r from-[#ff7a00] to-[#ffb000]" : "bg-white/10 text-slate-400"}`}>Continue to Secure Order <ArrowRight className="h-4 w-4" /></Link><p className="mt-3 text-center text-[11px] text-slate-400">Secure checkout · No password required</p></aside></div></section>
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-8 lg:grid-cols-[.9fr_1.1fr] lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">USA professional network · UI preview</p><h2 className="mt-3 text-3xl font-black">Build a more established professional network</h2><p className="mt-4 max-w-xl text-sm leading-7 text-slate-300">For founders, consultants, recruiters, agencies, B2B profiles and entrepreneurs, a clear public profile and visible professional network can help create a more considered first impression. This service does not promise outcomes such as leads, jobs, sales or engagement.</p><div className="mt-6 space-y-4">{[["Public profile first", "Keep your profile accessible and make your expertise clear."], ["Transparent order details", "Review the current rate, quantity limits and delivery estimate before checkout."], ["Track the workflow", "Use the SocialRUSH dashboard to keep the order status organized."]].map(([title, body]) => <div key={title} className="flex gap-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-orange-300"/><div><h3 className="font-black">{title}</h3><p className="mt-1 text-sm text-slate-300">{body}</p></div></div>)}</div></div><JourneyPreview /></div></section>
    <section className="border-y border-white/10 bg-[#0d131e] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">How it works</p><h2 className="mt-3 text-3xl font-black">A clear, secure order journey</h2><ol className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{["Choose LinkedIn USA Connections", "Select a valid quantity", "Add the required public LinkedIn URL", "Review live price and service details", "Continue to secure checkout", "Track the order in your dashboard"].map((step, index) => <li key={step} className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><span className="text-xs font-black text-orange-300">0{index + 1}</span><p className="mt-3 text-sm font-black">{step}</p></li>)}</ol></div></section>
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{[[LockKeyhole,"Public Link Only","SocialRUSH never requires LinkedIn credentials."],[WalletCards,"Transparent Pricing","The order builder calculates a total from the catalog rate."],[ShieldCheck,"Secure Checkout","Continue into the existing SocialRUSH order flow."],[Globe2,"USA-Focused Service","Use the provider-defined USA-targeted service option."],[Users,"Dashboard Tracking","Review your order status after checkout."],[Link2,"Support Details","Current delivery and refill/support terms are visible before ordering."]].map(([Icon,title,text]) => { const I = Icon as typeof LockKeyhole; return <article key={String(title)} className="rounded-2xl border border-[#0a66c2]/25 bg-[#0a66c2]/[.06] p-5"><I className="h-5 w-5 text-orange-300"/><h3 className="mt-4 font-black">{String(title)}</h3><p className="mt-2 text-sm leading-6 text-slate-300">{String(text)}</p></article>; })}</div><p className="mt-8 text-center text-sm text-slate-400">SocialRUSH never requires a LinkedIn password, OTP, email credential or recovery code.</p></div></section>
    <section className="border-y border-white/10 bg-[#0d131e] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-6xl gap-6 lg:grid-cols-2"><article><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Connections vs followers</p><h2 className="mt-3 text-3xl font-black">Choose the signal that fits your goal</h2><p className="mt-4 text-sm leading-7 text-slate-300">Connections and Followers are distinct LinkedIn actions. This page focuses on USA Connections for eligible personal profiles; use the follower service when your goal is follower-focused profile or company-page visibility.</p><Link href="/linkedin-followers" className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-xl border border-[#0a66c2]/40 bg-[#0a66c2]/10 px-4 text-sm font-black text-sky-100">Explore LinkedIn Followers <ArrowRight className="h-4 w-4" /></Link></article><div className="rounded-[2rem] border border-white/10 bg-white/[.035] p-6"><div className="border-b border-white/10 pb-4"><p className="font-black">LinkedIn Connections</p><p className="mt-2 text-sm text-slate-300">A professional network relationship, ordered here with a public personal profile URL.</p></div><div className="pt-4"><p className="font-black">LinkedIn Followers</p><p className="mt-2 text-sm text-slate-300">People who follow a profile or company page to receive its updates.</p></div></div></div></section>
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><p className="text-center text-xs font-black uppercase tracking-[.16em] text-orange-300">Service FAQ</p><h2 className="mt-3 text-center text-3xl font-black">LinkedIn USA Connections questions</h2><div className="mt-8 space-y-3">{faqs.map(([question, answer], index) => <details key={question} open={index === 0} className="group rounded-2xl border border-white/10 bg-white/[.035] p-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 text-sm font-black">{question}<ChevronDown className="h-5 w-5 text-orange-300 transition group-open:rotate-180"/></summary><p className="mt-4 border-t border-white/10 pt-4 text-sm leading-7 text-slate-300">{answer}</p></details>)}</div><div className="mt-10 flex flex-wrap justify-center gap-3"><Link href="/pricing" className="rounded-xl border border-white/15 px-5 py-3 text-sm font-black">Pricing</Link><Link href="/packages?platform=linkedin" className="rounded-xl border border-white/15 px-5 py-3 text-sm font-black">LinkedIn packages</Link><Link href="/linkedin-growth-india" className="rounded-xl border border-white/15 px-5 py-3 text-sm font-black">LinkedIn growth hub</Link><Link href="/blog" className="rounded-xl border border-white/15 px-5 py-3 text-sm font-black">LinkedIn guides</Link></div></div></section>
  </main></PublicShell>;
}

function NetworkPreview({ price }: { price: string }) { return <aside aria-label="Original professional network interface preview" className="relative overflow-hidden rounded-[2rem] border border-[#0a66c2]/35 bg-[linear-gradient(145deg,#111d2d,#0d1016_52%,#1a130c)] p-5 shadow-[0_32px_80px_-40px_rgba(10,102,194,.8)] sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-xl bg-[#0a66c2]"><LinkedIn className="h-6 w-6"/></span><div><p className="font-black">USA Network</p><p className="text-[10px] font-bold text-slate-400">Professional connections · UI preview</p></div></div><span className="rounded-full bg-[#0a66c2]/15 px-2.5 py-1 text-[9px] font-black text-sky-200">UI PREVIEW</span></div><div className="mt-5 rounded-2xl border border-white/10 bg-[#090e17] p-4"><div className="flex justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.14em] text-slate-400">Professional network</p><p className="mt-1 text-2xl font-black">United States</p></div><Globe2 className="h-9 w-9 text-orange-300"/></div><div className="mt-5 flex items-center justify-between"><span className="grid h-12 w-12 place-items-center rounded-full border border-sky-300/40 bg-[#0a66c2]/20"><Users className="h-5 w-5 text-sky-100"/></span><span className="h-px flex-1 bg-gradient-to-r from-[#0a66c2] to-orange-300"/><span className="grid h-10 w-10 place-items-center rounded-full border border-orange-300/40 bg-orange-300/10"><Users className="h-4 w-4 text-orange-200"/></span><span className="h-px flex-1 bg-gradient-to-r from-orange-300 to-[#0a66c2]"/><span className="grid h-12 w-12 place-items-center rounded-full border border-sky-300/40 bg-[#0a66c2]/20"><Users className="h-5 w-5 text-sky-100"/></span></div></div><div className="mt-4 grid grid-cols-2 gap-2 text-xs">{[["Starting rate", `${price} / 1K`],["Delivery", service?.deliveryTime ?? ""],["Destination","Personal profile"],["Service","USA connections"]].map(([label,value]) => <div key={label} className="rounded-xl border border-white/[.08] bg-white/[.035] p-3"><p className="text-slate-400">{label}</p><p className="mt-1 font-black">{value}</p></div>)}</div><p className="mt-4 text-[10px] leading-5 text-slate-500">Illustrative network UI only. It does not represent a customer account or guaranteed result.</p></aside>; }
function JourneyPreview() { return <div className="rounded-[2rem] border border-[#0a66c2]/25 bg-[#f6f8fb] p-5 text-slate-900 shadow-[0_28px_70px_-44px_rgba(10,102,194,.9)]"><div className="flex items-center justify-between border-b border-slate-200 pb-4"><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[#0a66c2] text-white"><LinkedIn className="h-4 w-4"/></span><div><p className="text-sm font-black">Professional Network Overview</p><p className="text-[10px] text-slate-500">Original illustrative UI preview</p></div></div><span className="text-[10px] font-black text-[#0a66c2]">USA</span></div><div className="mt-5 grid grid-cols-4 gap-2 text-center text-[10px] font-black"><span className="rounded-xl bg-slate-100 px-2 py-3">Profile</span><span className="rounded-xl bg-[#0a66c2] px-2 py-3 text-white">Connections</span><span className="rounded-xl bg-slate-100 px-2 py-3">Presence</span><span className="rounded-xl bg-slate-100 px-2 py-3">Content</span></div><div className="mt-5 rounded-2xl bg-[linear-gradient(135deg,#082d56,#0a66c2,#d98b28)] p-5 text-white"><p className="text-[10px] font-black uppercase tracking-wider text-sky-100">Network activity preview</p><div className="mt-4 flex h-20 items-end gap-2">{[36,53,44,70,61,82,68].map((height,index) => <i key={index} className="w-full rounded-t bg-white/80" style={{height: `${height}%`}}/>)}</div></div><div className="mt-4 grid grid-cols-3 gap-2">{["Profile clarity","Public link","Order tracking"].map(item => <span key={item} className="rounded-xl border border-slate-200 p-3 text-center text-[10px] font-bold">{item}</span>)}</div></div>; }
