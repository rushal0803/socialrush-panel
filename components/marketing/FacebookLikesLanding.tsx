"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowRight, BarChart3, BriefcaseBusiness, Check, ChevronDown, CircleCheck, Heart, Link2, LockKeyhole, MessageCircle, Minus, Pause, Play, Plus, ShieldCheck, Sparkles, Users, WalletCards } from "lucide-react";
import { FaFacebookF as Facebook } from "react-icons/fa6";
import PublicShell from "@/components/marketing/PublicShell";
import { getServiceById } from "@/lib/smm-service-catalog";
import { calculateServiceTotal } from "@/lib/service-pricing";
import { formatCurrency } from "@/lib/currency";
import { usePreferredCurrency } from "@/lib/currency/use-currency";
import { linkRules, validateCampaignLink } from "@/lib/order-service-experience";

const service = getServiceById("facebook-likes");
const whatsapp = "https://wa.me/918860330771?text=Hi%20SocialRUSH%2C%20I%20need%20help%20with%20Facebook%20Likes";
const faqs = [
  ["Do I need my Facebook password?", "No. SocialRUSH only needs the public Facebook post or video URL required by this service. Never share a password, login, or OTP."],
  ["Which Facebook post link should I submit?", "Submit the exact public Facebook post or video link. Before continuing, check that it opens publicly and points to the intended content."],
  ["Can I order Likes for any public Facebook post?", "You can submit an eligible public Facebook post or video link that meets the validation requirements shown in the order builder."],
  ["How is the price calculated?", "Your total is calculated from the active catalog rate and selected quantity, then shown before you enter the existing secure order flow."],
  ["How long does delivery take?", `The active service estimate is ${service?.deliveryTime ?? "shown before checkout"}. Timing can vary with quantity and post availability.`],
  ["Is refill/support available?", `The active catalog lists ${service?.refillPolicy ?? "the current service terms"}. Review the service information before ordering.`],
  ["Can I track my order?", "Yes. After secure checkout, use your SocialRUSH dashboard to review the order status and history."],
  ["What happens if I submit the wrong link?", "Verify the public URL before placing the order. For help with an existing order, contact support through your dashboard."],
] as const;

const displayQuantity = (value: number) => value >= 1000 ? `${value / 1000}K` : value.toLocaleString("en-IN");

export default function FacebookLikesLanding() {
  const { currency } = usePreferredCurrency("INR");
  const [playing, setPlaying] = useState(true);
  const [pulse, setPulse] = useState(42);
  useEffect(() => {
    if (!playing) return;
    const id = window.setInterval(() => setPulse((value) => value >= 91 ? 35 : value + 1), 125);
    return () => window.clearInterval(id);
  }, [playing]);
  if (!service) return null;
  const price = formatCurrency(service.pricePer1000, currency);
  return <PublicShell tone="light3d"><main className="overflow-hidden bg-[#0b0d12] text-white">
    <section className="relative border-b border-white/10 px-4 pb-14 pt-12 sm:px-6 lg:px-8 lg:pb-20 lg:pt-16">
      <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_88%_13%,rgba(24,119,242,.2),transparent_25%),radial-gradient(circle_at_14%_8%,rgba(255,151,34,.18),transparent_25%)]" />
      <div className="relative mx-auto grid max-w-7xl items-center gap-9 lg:grid-cols-[.94fr_1.06fr]"><div>
        <span className="inline-flex items-center gap-2 rounded-full border border-[#1877f2]/35 bg-[#1877f2]/10 px-3 py-2 text-[10px] font-black tracking-[.16em] text-blue-100"><Facebook className="h-4 w-4 fill-current" />FACEBOOK LIKES SERVICE</span>
        <h1 className="mt-5 max-w-3xl text-4xl font-black leading-[1.04] tracking-[-.055em] sm:text-5xl lg:text-6xl">Buy Facebook Likes <span className="bg-gradient-to-r from-orange-300 to-amber-200 bg-clip-text text-transparent">in India</span></h1>
        <p className="mt-5 max-w-xl text-base leading-7 text-slate-300 sm:text-lg">Order Likes for a public Facebook post, see transparent live pricing, and follow delivery details from your SocialRUSH dashboard.</p>
        <div className="mt-7 flex flex-col gap-3 min-[420px]:flex-row min-[420px]:flex-wrap"><a href="#order" className="inline-flex min-h-12 items-center justify-center rounded-xl bg-gradient-to-r from-[#ff7a00] to-[#ffb000] px-6 text-sm font-black shadow-[0_15px_32px_-12px_rgba(255,147,0,.8)]">Start Your Order</a><a href="#order" className="inline-flex min-h-12 items-center justify-center rounded-xl border border-white/15 bg-white/[.05] px-6 text-sm font-black">View Facebook Packages</a><a href={whatsapp} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl border border-emerald-400/25 bg-emerald-400/[.07] px-5 text-sm font-black text-emerald-100"><MessageCircle className="h-4 w-4" />WhatsApp</a></div>
        <div className="mt-6 flex flex-wrap gap-x-5 gap-y-2 text-xs font-bold text-slate-300">{["No Password Required", "Transparent Pricing", "Order Tracking"].map((text) => <span key={text} className="flex items-center gap-1.5"><Check className="h-4 w-4 text-orange-300" />{text}</span>)}</div>
      </div><EngagementDashboard price={price} pulse={pulse} playing={playing} /></div>
    </section>
    <section className="border-b border-white/10 bg-[#11141b] px-4 py-4 sm:px-6"><div className="mx-auto flex max-w-7xl flex-wrap justify-center gap-x-7 gap-y-3 text-xs font-bold text-slate-300">{[[LockKeyhole,"No password required"],[Link2,"Public post link"],[WalletCards,"Secure checkout"],[BarChart3,"Dashboard tracking"],[ShieldCheck,"Live service details"]].map(([Icon,label]) => { const IconComponent = Icon as typeof LockKeyhole; return <span className="flex items-center gap-2" key={label as string}><IconComponent className="h-4 w-4 text-orange-300" />{label as string}</span>; })}</div></section>
    <OrderBuilder />
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-8 lg:grid-cols-[.82fr_1.18fr] lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Interactive UI Preview</p><h2 className="mt-3 text-3xl font-black">Preview the Facebook engagement experience</h2><p className="mt-4 max-w-md text-sm leading-7 text-slate-300">An original interface preview only—not a customer page or performance result. The play control animates demo engagement activity.</p><button type="button" onClick={() => setPlaying((value) => !value)} className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-xl border border-[#1877f2]/35 bg-[#1877f2]/10 px-4 text-sm font-black text-blue-100">{playing ? <Pause className="h-4 w-4" /> : <Play className="h-4 w-4" />}{playing ? "Pause Preview" : "Play Preview"}</button></div><PostPreview pulse={pulse} playing={playing} /></div></section>
    <ContentSections price={price} />
    <section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><p className="text-center text-xs font-black uppercase tracking-[.16em] text-orange-300">Helpful answers</p><h2 className="mt-3 text-center text-3xl font-black">Facebook Likes FAQs</h2><div className="mt-7 space-y-3">{faqs.map(([question, answer], index) => <details key={question} open={index === 0} className="group rounded-2xl border border-white/10 bg-white/[.035] p-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 text-sm font-black"><span>{question}</span><ChevronDown className="h-5 w-5 text-orange-300 transition group-open:rotate-180" /></summary><p className="mt-3 border-t border-white/10 pt-3 text-sm leading-7 text-slate-300">{answer}</p></details>)}</div></div></section>
    <section className="px-4 pb-16 sm:px-6 lg:px-8 lg:pb-20"><div className="mx-auto max-w-6xl rounded-[2rem] border border-orange-400/25 bg-[radial-gradient(circle_at_82%_30%,rgba(24,119,242,.18),transparent_21%),linear-gradient(120deg,#19140d,#10151e)] p-7 text-center sm:p-10"><p className="text-sm font-bold text-orange-200">Starting from {price} / 1K</p><h2 className="mt-2 text-3xl font-black">Ready to build your Facebook Likes order?</h2><p className="mt-3 text-sm text-slate-300">No password required · Live pricing · Dashboard tracking</p><div className="mt-6 flex flex-col justify-center gap-3 min-[420px]:flex-row"><a href="#order" className="inline-flex min-h-12 items-center justify-center rounded-xl bg-gradient-to-r from-[#ff7a00] to-[#ffb000] px-6 text-sm font-black">Start Your Order</a><Link href="/packages?platform=facebook&service=likes" className="inline-flex min-h-12 items-center justify-center rounded-xl border border-[#1877f2]/35 px-6 text-sm font-black text-blue-100">Explore Facebook Services</Link></div></div></section>
  </main></PublicShell>;
}

function EngagementDashboard({ price, pulse, playing }: { price: string; pulse: number; playing: boolean }) { return <aside className="relative overflow-hidden rounded-[2rem] border border-[#1877f2]/30 bg-[linear-gradient(145deg,#151a26,#0d1016_52%,#15110e)] p-5 shadow-[0_32px_80px_-40px_rgba(24,119,242,.8)] sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-full bg-[#1877f2]"><Facebook className="h-6 w-6 fill-current" /></span><div><p className="font-black">Facebook Likes</p><p className="text-[10px] font-bold text-slate-400">Post engagement dashboard · UI Preview</p></div></div><span className="rounded-full border border-emerald-400/25 bg-emerald-400/10 px-2.5 py-1 text-[9px] font-black text-emerald-300">LIVE SERVICE</span></div><MiniPost pulse={pulse} playing={playing} dark /><div className="mt-4 grid grid-cols-2 gap-2 text-xs">{[["Starting from",`${price} / 1K`],["Delivery",service?.deliveryTime ?? "—"],["Refill/support",service?.refillPolicy ?? "—"],["Public post link","Required"]].map(([label,value]) => <div key={label} className="rounded-xl border border-white/[.08] bg-white/[.035] p-3"><p className="text-slate-400">{label}</p><p className="mt-1 font-black">{value}</p></div>)}</div><a href="#order" className="mt-4 flex min-h-12 items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-[#ff7a00] to-[#ffb000] text-sm font-black">Build Your Order <ArrowRight className="h-4 w-4" /></a></aside>; }

function OrderBuilder() { const { currency } = usePreferredCurrency("INR"); const [quantity, setQuantity] = useState(service?.minQuantity ?? 100); const [link, setLink] = useState(""); const [attempted, setAttempted] = useState(false); if (!service) return null; const step = service.quantityStep ?? 1; const update = (raw: number) => setQuantity(Math.max(service.minQuantity, Math.min(service.maxQuantity, Math.round(raw / step) * step))); const presets = Array.from(new Set([service.minQuantity, 500, 1000, 5000, 10000].filter((value) => value >= service.minQuantity && value <= service.maxQuantity))); const numericValid = Number.isInteger(quantity) && quantity >= service.minQuantity && quantity <= service.maxQuantity && quantity % step === 0; const linkError = link.trim() ? validateCampaignLink(link, linkRules[service.code]) : ""; const valid = numericValid && Boolean(link.trim()) && !linkError; const href = `/dashboard/new-order?platform=facebook&service=${service.code}&resume=1&quantity=${quantity}&link=${encodeURIComponent(link.trim())}`; return <section id="order" className="scroll-mt-20 border-y border-white/10 bg-[#101218] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-[minmax(0,1fr)_390px]"><article className="rounded-[2rem] border border-white/10 bg-[radial-gradient(circle_at_93%_4%,rgba(24,119,242,.18),transparent_24%),linear-gradient(145deg,#191b24,#101116)] p-6 sm:p-8"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">Quick order builder</p><h2 className="mt-3 text-2xl font-black sm:text-3xl">Build Your Facebook Likes Order</h2><p className="mt-2 text-sm text-slate-300">Choose your quantity and see your total instantly.</p><div className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-5">{presets.map((value) => <button type="button" key={value} onClick={() => update(value)} className={`min-h-18 rounded-2xl border p-3 text-left ${quantity === value ? "border-[#1877f2] bg-[#1877f2]/15" : "border-white/10 bg-white/[.035] text-slate-200"}`}><b>{displayQuantity(value)}</b><span className="mt-1 block text-[10px] font-bold uppercase text-blue-200">Likes</span></button>)}</div><label className="mt-7 block text-sm font-black">Choose Likes<span className="mt-2 flex overflow-hidden rounded-2xl border border-white/10 bg-black/25 focus-within:border-[#1877f2]"><button type="button" aria-label="Decrease Likes quantity" onClick={() => update(quantity - step)} className="grid min-h-14 w-14 place-items-center text-orange-300"><Minus className="h-4 w-4" /></button><input aria-label="Custom Likes quantity" inputMode="numeric" value={quantity || ""} onChange={(event) => setQuantity(event.target.value === "" ? 0 : Number(event.target.value.replace(/\D/g, "")))} className="min-w-0 flex-1 bg-transparent text-center outline-none" /><button type="button" aria-label="Increase Likes quantity" onClick={() => update(quantity + step)} className="grid min-h-14 w-14 place-items-center text-orange-300"><Plus className="h-4 w-4" /></button></span></label><p className={`mt-2 text-xs ${attempted && !numericValid ? "font-bold text-red-300" : "text-slate-400"}`}>{attempted && !numericValid ? `Enter a whole quantity from ${displayQuantity(service.minQuantity)} to ${displayQuantity(service.maxQuantity)}.` : `Min ${displayQuantity(service.minQuantity)} • Max ${displayQuantity(service.maxQuantity)}`}</p><label className="mt-7 block text-sm font-black">Public Facebook Post URL<span className={`mt-2 flex rounded-2xl border bg-black/25 focus-within:border-[#1877f2] ${link.trim() && !linkError ? "border-emerald-400/50" : attempted && (!link.trim() || linkError) ? "border-red-400/60" : "border-white/10"}`}><Facebook className="m-4 h-5 w-5 shrink-0 fill-[#1877f2] text-[#1877f2]" /><input value={link} onChange={(event) => setLink(event.target.value)} placeholder={linkRules[service.code].placeholder} className="min-w-0 flex-1 bg-transparent pr-4 outline-none" /></span></label><p className={`mt-2 text-xs leading-6 ${attempted && (!link.trim() || linkError) ? "font-bold text-red-300" : link.trim() && !linkError ? "text-emerald-300" : "text-slate-400"}`}>{link.trim() && !linkError ? "✓ Valid public Facebook link" : attempted && (!link.trim() || linkError) ? linkError || "Enter a valid public Facebook post URL." : "Public links only. We never ask for Facebook passwords, logins, or credentials."}</p></article><aside className="rounded-[2rem] border border-orange-400/25 bg-[linear-gradient(160deg,#21150e,#11151d_50%,#0b0c10)] p-6 lg:sticky lg:top-28 lg:h-fit"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">Live order summary</p><h3 className="mt-2 text-xl font-black">Facebook Likes</h3><dl className="mt-6 space-y-3 text-sm">{[["Quantity",displayQuantity(quantity)],["Live rate",`${formatCurrency(service.pricePer1000, currency)} / 1K`],["Delivery",service.deliveryTime],["Refill/support",service.refillPolicy],["Facebook Link Status",link.trim() && !linkError ? "Ready" : "Required"]].map(([label,value]) => <div key={label} className="flex justify-between gap-4"><dt className="text-slate-400">{label}</dt><dd className="text-right font-black">{value}</dd></div>)}</dl><div className="mt-6 rounded-2xl border border-orange-400/20 bg-black/25 p-4"><p className="text-[10px] font-black uppercase text-orange-200">Total</p><p className="mt-2 text-3xl font-black">{formatCurrency(calculateServiceTotal(service.code, quantity), currency)}</p></div><Link href={valid ? href : "#order"} onClick={(event) => { if (!valid) { event.preventDefault(); setAttempted(true); } }} aria-disabled={!valid} className={`mt-6 inline-flex min-h-14 w-full items-center justify-center gap-2 rounded-2xl text-sm font-black ${valid ? "bg-gradient-to-r from-[#ff7a00] to-[#ffb000]" : "bg-white/10 text-slate-400"}`}>Continue to Secure Order <ArrowRight className="h-4 w-4" /></Link><p className="mt-3 text-center text-[11px] text-slate-400">Secure checkout • No password required</p></aside></div></section>; }

function MiniPost({ pulse, playing, dark = false }: { pulse: number; playing: boolean; dark?: boolean }) { return <div className={`mt-5 overflow-hidden rounded-2xl border p-3 ${dark ? "border-white/10 bg-[#f7f9fc] text-slate-900" : "border-slate-200 bg-white text-slate-900"}`}><div className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-full bg-[#1877f2] text-white"><Facebook className="h-4 w-4 fill-current" /></span><div><p className="text-xs font-black">Your Page</p><p className="text-[9px] text-slate-500">Just now · Public · UI Preview</p></div></div><p className="mt-3 text-xs font-semibold">New ideas, fresh details, and more to explore.</p><div className="relative mt-3 h-24 overflow-hidden rounded-xl bg-[linear-gradient(135deg,#103f79,#1877f2_46%,#f4a42b)] p-3 text-white"><span className="text-[8px] font-black tracking-[.18em]">EXPLORE WHAT’S NEW</span><div className="absolute bottom-3 right-4 grid h-11 w-11 place-items-center rounded-xl border border-white/30 bg-white/15"><Sparkles className="h-6 w-6" /></div><div className="mt-3 h-1.5 w-2/3 rounded bg-white/45" /></div><div className="mt-3 flex items-center justify-between text-[10px] text-slate-500"><span className="flex items-center gap-1"><Heart className="h-3.5 w-3.5 fill-red-500 text-red-500" />{(1240 + pulse * 4).toLocaleString("en-IN")} demo likes</span><span>UI Preview</span></div><div className="mt-2 flex justify-around border-t border-slate-200 pt-2 text-[10px] font-bold text-slate-500"><span className="flex items-center gap-1 text-[#1877f2]"><Heart className={`h-3.5 w-3.5 ${playing ? "fill-[#1877f2]" : ""}`} />Like</span><span>Comment</span><span>Share</span></div></div>; }

function PostPreview({ pulse, playing }: { pulse: number; playing: boolean }) { return <div className="rounded-[2rem] border border-[#1877f2]/25 bg-[#f7f9fc] p-4 text-slate-900 shadow-[0_32px_80px_-42px_rgba(24,119,242,.8)]"><div className="flex items-center justify-between"><p className="text-xs font-black text-[#1877f2]">Facebook post activity</p><span className="rounded-full bg-blue-50 px-2 py-1 text-[9px] font-black text-[#1877f2]">INTERACTIVE UI PREVIEW</span></div><MiniPost pulse={pulse} playing={playing} /><div className="mt-4 rounded-xl border border-blue-100 bg-blue-50 p-3"><div className="flex justify-between text-xs"><span className="font-black text-[#1877f2]">Like activity · Demo only</span><span>{playing ? "Animating" : "Paused"}</span></div><div className="mt-3 flex h-16 items-end gap-2">{[38,54,42,72,58,80,66].map((height, index) => <i key={index} className="w-full rounded-t bg-gradient-to-t from-[#1877f2] to-[#79b3ff] transition-all duration-500" style={{ height: `${playing ? Math.min(94, height + ((pulse + index * 9) % 17)) : height}%` }} />)}</div></div></div>; }

function ContentSections({ price }: { price: string }) { const bento = [["Transparent Live Pricing",`Current catalog rate: ${price} / 1K`,WalletCards],["Public Post-Link Ordering","Submit the public Facebook post or video URL.",Link2],["Secure Checkout","Continue through the protected SocialRUSH flow.",ShieldCheck],["Dashboard Tracking","Keep order status and history organized.",BarChart3],["No Password Required","Facebook credentials are never requested.",LockKeyhole],["Refill / Support Information",service?.refillPolicy ?? "Shown with service",CircleCheck]] as const; const audiences = [[Users,"Creators","Keep post campaigns organized."],[Facebook,"Facebook Pages","Support visible post engagement."],[BriefcaseBusiness,"Local Businesses","Present updates with confidence."],[Sparkles,"Brands","Coordinate campaign details clearly."],[MessageCircle,"Community Managers","Manage public-post campaigns."],[Users,"Social Media Teams","Keep delivery tracking in one place."]]; return <><section className="border-y border-white/10 bg-[#101218] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Why SocialRUSH</p><h2 className="mt-3 text-3xl font-black">Clarity throughout your order</h2><div className="mt-7 grid gap-3 md:grid-cols-4">{bento.map(([title,text,Icon], index) => <article key={title} className={`rounded-2xl border border-white/10 bg-white/[.035] p-5 ${index === 0 ? "md:col-span-2 md:row-span-2 bg-[radial-gradient(circle_at_90%_12%,rgba(24,119,242,.18),transparent_35%)]" : ""}`}><Icon className="h-5 w-5 text-orange-300" /><h3 className="mt-4 font-black">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-300">{text}</p></article>)}</div></div></section><section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Who it’s for</p><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{audiences.map(([Icon,title,text]) => { const IconComponent = Icon as typeof Users; return <article key={title as string} className="flex gap-3 rounded-2xl border border-white/10 bg-white/[.035] p-4"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-[#1877f2]/15 text-blue-200"><IconComponent className="h-5 w-5" /></span><div><h3 className="font-black">{title as string}</h3><p className="mt-1 text-sm text-slate-300">{text as string}</p></div></article>; })}</div></div></section><section className="border-y border-white/10 bg-[#101218] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">How it works</p><ol className="mt-7 grid gap-4 md:grid-cols-3 lg:grid-cols-6">{["Choose Facebook Likes","Select Quantity","Paste Public Post Link","Review Live Total","Pay Securely","Track Your Order"].map((text,index) => <li key={text} className="border-l border-orange-400/45 pl-4 lg:border-l-0 lg:border-t lg:pt-5 lg:pl-0"><span className="text-xs font-black text-orange-300">0{index + 1}</span><p className="mt-2 text-sm font-black">{text}</p></li>)}</ol></div></section><section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-2"><PostPreview pulse={42} playing={false} /><div><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Built for public Facebook post engagement</p><h2 className="mt-3 text-3xl font-black">Designed around a clear public-link workflow</h2><div className="mt-6 space-y-4">{[["Public Post Ordering","Use the public destination required by the service."],["Clear Campaign Details","Review pricing and service information before continuing."],["Trackable Delivery","Track your SocialRUSH order from the existing dashboard."]].map(([title,text]) => <div key={title} className="flex gap-3"><CircleCheck className="mt-0.5 h-5 w-5 shrink-0 text-orange-300" /><div><h3 className="font-black">{title}</h3><p className="mt-1 text-sm text-slate-300">{text}</p></div></div>)}</div></div></div></section><section className="bg-[#101218] px-4 py-12 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl rounded-2xl border border-white/10 bg-white/[.035] p-6"><h2 className="text-2xl font-black">Good to Know</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{["Submit the correct public Facebook post link and keep it accessible where required.",`Current delivery estimate: ${service?.deliveryTime ?? "shown before checkout"}.`,`Current refill/support information: ${service?.refillPolicy ?? "shown before checkout"}.`,`Verify the submitted link before ordering—no Facebook password is required.`].map((text) => <p key={text} className="flex gap-2 text-sm leading-6 text-slate-300"><Check className="mt-1 h-4 w-4 shrink-0 text-emerald-300" />{text}</p>)}</div></div></section><section className="px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-300">Related Facebook services</p><div className="mt-6 grid gap-4 md:grid-cols-3">{[["Facebook Followers","/facebook-followers","Public page and profile growth service"],["Facebook Views","/services?platform=facebook","Explore active Facebook video services"],["Facebook Packages","/packages?platform=facebook","Compare current Facebook catalog packages"]].map(([title,href,text]) => <Link key={title} href={href} className="group rounded-2xl border border-[#1877f2]/25 bg-[#1877f2]/[.06] p-5 transition hover:-translate-y-1"><Facebook className="h-6 w-6 fill-[#1877f2] text-[#1877f2]" /><h3 className="mt-4 font-black">{title}</h3><p className="mt-2 text-sm text-slate-300">{text}</p><span className="mt-4 inline-flex items-center gap-1 text-xs font-black text-orange-300">Explore <ArrowRight className="h-3.5 w-3.5" /></span></Link>)}</div></div></section></>; }
