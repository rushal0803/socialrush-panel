"use client";

import Link from "next/link";
import { AnimatePresence, motion } from "framer-motion";
import { type FormEvent, useId, useState } from "react";
import { ArrowRight, Check, ChevronDown, CircleHelp, CreditCard, ExternalLink, Mail, MessageCircle, Package, ShieldCheck, Sparkles, UserRound } from "lucide-react";
import BlogShell from "@/components/marketing/blog/BlogShell";
import { agencyServices } from "@/lib/marketing/content";

type Category = "order" | "payment" | "guidance" | "account" | "general";
type FormStatus = "idle" | "loading" | "success" | "error";

const whatsappUrl = "https://wa.me/918860330771";
const supportEmail = "support@getsocialrush.com";

const paths: { key: Category; title: string; text: string; icon: typeof Package; wide?: boolean }[] = [
  { key: "order", title: "Order support", text: "Delivery, refill, status, or an existing order.", icon: Package, wide: true },
  { key: "payment", title: "Payment & wallet", text: "Questions about a payment or wallet transaction.", icon: CreditCard },
  { key: "guidance", title: "Service guidance", text: "Help choosing a service before you place an order.", icon: Sparkles },
  { key: "account", title: "Account help", text: "Assistance with your SocialRUSH account.", icon: UserRound },
  { key: "general", title: "General enquiry", text: "Other questions for the SocialRUSH team.", icon: CircleHelp },
];

const faqs = [
  ["How do I contact SocialRUSH support?", "Use this form, WhatsApp support, or the support area in your dashboard. Include the details that relate to your request."],
  ["What should I include for an order issue?", "Share the correct Order ID and describe the issue. This helps the team review the relevant order."],
  ["Where can I find my Order ID?", "Signed-in customers can find order records in the SocialRUSH dashboard."],
  ["Do I need to share my social media password?", "No. SocialRUSH only needs the relevant public destination for an order. Never share passwords, recovery codes, or OTPs."],
  ["Can I contact support before ordering?", "Yes. Choose Service guidance or General enquiry and tell us what you would like to achieve."],
];

const fieldClass = "mt-2 min-h-12 w-full rounded-xl border border-white/10 bg-[#090b12] px-4 text-sm text-white outline-none transition placeholder:text-slate-500 focus:border-orange-300 focus:ring-2 focus:ring-orange-300/20";

export default function ContactPageContent() {
  const [category, setCategory] = useState<Category>("general");
  const [status, setStatus] = useState<FormStatus>("idle");
  const [notice, setNotice] = useState("");
  const [openFaq, setOpenFaq] = useState(0);
  const [touched, setTouched] = useState<Record<string, boolean>>({});
  const formId = useId();
  const needsOrder = category === "order" || category === "payment";

  function choosePath(next: Category) {
    setCategory(next);
    document.getElementById("support-form")?.scrollIntoView({ behavior: "smooth", block: "center" });
  }

  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    const form = event.currentTarget;
    const values = new FormData(form);
    const required = ["name", "email", "whatsapp", "service", "message"];
    if (needsOrder) required.push("orderReference");
    const missing = required.some((key) => !String(values.get(key) ?? "").trim());
    setTouched(Object.fromEntries(required.map((key) => [key, true])));
    if (missing) { setStatus("error"); setNotice("Please complete the highlighted details so we can help."); return; }
    setStatus("loading"); setNotice("Sending your message…");
    const reference = String(values.get("orderReference") || "").trim();
    if (reference) values.set("message", `[${category === "payment" ? "Payment / order reference" : "Order ID"}: ${reference}]\n\n${String(values.get("message") || "")}`);
    try {
      const response = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(values.entries())) });
      const result = await response.json().catch(() => null) as { message?: string; error?: string } | null;
      if (!response.ok) throw new Error(result?.error || "We couldn't send your message. Please try again.");
      form.reset(); setStatus("success"); setNotice(result?.message || "Message sent successfully."); setTouched({});
    } catch (error) { setStatus("error"); setNotice(error instanceof Error ? error.message : "We couldn't send your message. Please try again."); }
  }

  const error = (name: string) => touched[name] ? <span className="mt-1.5 block text-xs text-rose-300">Please enter {name === "whatsapp" ? "a valid WhatsApp number" : name === "service" ? "a service" : name === "orderReference" ? "your Order ID" : name === "message" ? "how we can help" : `your ${name}`}.</span> : null;

  return <BlogShell><main className="relative overflow-x-clip bg-[#06070b] pb-20 text-white">
    <div className="pointer-events-none absolute inset-x-0 top-0 h-[760px] bg-[radial-gradient(circle_at_15%_12%,rgba(255,122,0,.22),transparent_28%),radial-gradient(circle_at_85%_22%,rgba(37,99,235,.16),transparent_25%)]" />
    <section className="relative mx-auto grid max-w-7xl gap-10 px-4 pb-14 pt-10 sm:px-6 lg:grid-cols-[.95fr_1.05fr] lg:px-8 lg:pb-20 lg:pt-16">
      <div className="self-center"><p className="inline-flex items-center gap-2 rounded-full border border-orange-300/30 bg-orange-400/10 px-3 py-1.5 text-[10px] font-black tracking-[.18em] text-orange-200"><span className="h-1.5 w-1.5 rounded-full bg-orange-300" />SOCIALRUSH SUPPORT</p>
        <h1 className="mt-5 max-w-xl text-4xl font-black tracking-tight sm:text-5xl lg:text-6xl">How can we <span className="text-orange-300">help?</span></h1>
        <p className="mt-5 max-w-xl text-base leading-8 text-slate-300">Whether you have a question about an order, payment, service or your SocialRUSH account, choose the right support path and send us the details.</p>
        <div className="mt-7 flex flex-wrap gap-3"><a href="#support-form" className="inline-flex min-h-12 items-center gap-2 rounded-xl bg-gradient-to-r from-[#ff7600] to-[#ffb000] px-5 text-sm font-black shadow-[0_14px_30px_-12px_rgba(255,122,0,.8)] transition hover:-translate-y-0.5">Get Support <ArrowRight className="h-4 w-4" /></a><Link href="/services" className="inline-flex min-h-12 items-center rounded-xl border border-white/15 bg-white/[.04] px-5 text-sm font-bold text-slate-100 transition hover:border-orange-300/50">Explore Services</Link><a href={whatsappUrl} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-12 items-center gap-2 rounded-xl border border-emerald-300/25 bg-emerald-400/[.08] px-5 text-sm font-bold text-emerald-100 transition hover:bg-emerald-400/[.14]"><MessageCircle className="h-4 w-4" />WhatsApp Us</a></div>
        <p className="mt-6 text-xs font-semibold text-slate-400">Clear support paths <span className="mx-1 text-orange-300">•</span> Order-specific assistance <span className="mx-1 text-orange-300">•</span> Secure account support</p>
      </div>
      <SupportPreview />
    </section>

    <section className="relative mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="max-w-xl"><p className="text-[10px] font-black tracking-[.18em] text-orange-300">SUPPORT PATHS</p><h2 className="mt-3 text-3xl font-black">What can we help you with?</h2></div><div className="mt-7 grid gap-3 md:grid-cols-4">{paths.map((path, index) => { const Icon = path.icon; const active = category === path.key; return <button key={path.key} type="button" onClick={() => choosePath(path.key)} className={`${path.wide ? "md:col-span-2" : ""} group min-h-44 rounded-3xl border p-5 text-left transition motion-reduce:transition-none ${active ? "border-orange-300/70 bg-gradient-to-br from-orange-500/20 to-[#111526] shadow-[0_18px_40px_-24px_rgba(255,122,0,.9)]" : "border-white/10 bg-[#0d0f17] hover:-translate-y-1 hover:border-white/25"}`}><span className="flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-white/[.07] text-orange-300"><Icon className="h-5 w-5" /></span><span className="text-xs font-black text-slate-500">0{index + 1}</span></span><span className="mt-5 block text-base font-black">{path.title}</span><span className="mt-2 block text-sm leading-6 text-slate-400">{path.text}</span><span className="mt-4 inline-flex items-center gap-1 text-xs font-bold text-orange-200">{active ? "Selected" : "Choose path"}<ArrowRight className="h-3.5 w-3.5 transition group-hover:translate-x-1" /></span></button>; })}</div></section>

    <section id="support-form" className="relative mx-auto grid max-w-7xl gap-5 px-4 py-12 sm:px-6 lg:grid-cols-[1.12fr_.88fr] lg:px-8"><form onSubmit={submit} noValidate className="rounded-[28px] border border-white/10 bg-[#10131e] p-5 shadow-2xl sm:p-8"><p className="text-[10px] font-black tracking-[.18em] text-orange-300">SUPPORT REQUEST</p><h2 className="mt-3 text-3xl font-black">Send us a message</h2><p className="mt-2 text-sm leading-6 text-slate-400">Share the relevant details so we can understand your request clearly.</p><label className="hidden">Website<input name="website" tabIndex={-1} autoComplete="off" /></label><input type="hidden" name="source" value="/contact" />
      <div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-slate-200">Full name <span className="text-orange-300">*</span><input name="name" onBlur={() => setTouched((v) => ({ ...v, name: true }))} className={fieldClass} placeholder="Your name" />{error("name")}</label><label className="text-xs font-bold text-slate-200">Email address <span className="text-orange-300">*</span><input name="email" type="email" onBlur={() => setTouched((v) => ({ ...v, email: true }))} className={fieldClass} placeholder="you@example.com" />{error("email")}</label><label className="text-xs font-bold text-slate-200">WhatsApp number <span className="text-orange-300">*</span><input name="whatsapp" type="tel" onBlur={() => setTouched((v) => ({ ...v, whatsapp: true }))} className={fieldClass} placeholder="+91…" />{error("whatsapp")}</label><label className="text-xs font-bold text-slate-200">Service interested in <span className="text-orange-300">*</span><select name="service" defaultValue="" onBlur={() => setTouched((v) => ({ ...v, service: true }))} className={fieldClass}><option value="">Select a service</option>{agencyServices.map((service) => <option key={service.slug} value={service.name}>{service.name}</option>)}</select>{error("service")}</label></div>
      <div className="mt-4 rounded-2xl border border-white/[.07] bg-[#090b12] p-3"><p className="text-xs font-bold text-slate-300">Support category</p><div className="mt-2 flex flex-wrap gap-2">{paths.map((path) => <button type="button" key={path.key} onClick={() => setCategory(path.key)} className={`rounded-lg px-3 py-2 text-xs font-bold transition ${category === path.key ? "bg-orange-400 text-black" : "bg-white/[.06] text-slate-300 hover:bg-white/[.1]"}`}>{path.title}</button>)}</div></div>
      {needsOrder && <label className="mt-4 block text-xs font-bold text-slate-200">{category === "payment" ? "Transaction or Order reference" : "Order ID"} <span className="text-orange-300">*</span><input name="orderReference" onBlur={() => setTouched((v) => ({ ...v, orderReference: true }))} className={fieldClass} placeholder={category === "payment" ? "Your transaction or order reference" : "Your SocialRUSH Order ID"} />{error("orderReference")}</label>}
      <label className="mt-4 block text-xs font-bold text-slate-200">Message <span className="text-orange-300">*</span><textarea name="message" rows={5} onBlur={() => setTouched((v) => ({ ...v, message: true }))} className={`${fieldClass} min-h-32 py-3`} placeholder="Tell us how we can help." />{error("message")}</label>
      <button disabled={status === "loading"} type="submit" className="mt-6 inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-[#ff7600] to-[#ffb000] px-5 text-sm font-black text-white transition hover:brightness-110 disabled:cursor-wait disabled:opacity-70">{status === "loading" ? "Sending…" : "Send Message"}<ArrowRight className="h-4 w-4" /></button>
      <AnimatePresence>{notice && <motion.div initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} className={`mt-4 rounded-xl border p-3 text-sm ${status === "success" ? "border-emerald-300/30 bg-emerald-400/10 text-emerald-100" : status === "error" ? "border-rose-300/30 bg-rose-400/10 text-rose-100" : "border-orange-300/30 bg-orange-400/10 text-orange-100"}`} role="status" aria-live="polite">{status === "success" && <Check className="mr-2 inline h-4 w-4" />}{notice}{status === "success" && <span className="mt-3 flex flex-wrap gap-3"><Link href="/dashboard" className="font-bold underline">View Dashboard</Link><Link href="/services" className="font-bold underline">Explore Services</Link></span>}</motion.div>}</AnimatePresence></form>
      <aside className="space-y-5"><article className="overflow-hidden rounded-[28px] border border-orange-300/20 bg-[radial-gradient(circle_at_top_right,rgba(255,122,0,.18),transparent_45%),#10131e] p-6"><p className="text-[10px] font-black tracking-[.18em] text-orange-300">ALREADY PLACED AN ORDER?</p><h2 className="mt-3 text-2xl font-black">Bring the right details.</h2><p className="mt-3 text-sm leading-6 text-slate-400">Include the correct Order ID when your request relates to an existing order.</p><OrderPreview /><Link href="/dashboard/orders" className="mt-5 inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/15 px-4 text-sm font-bold transition hover:border-orange-300/50">View My Orders <ArrowRight className="h-4 w-4" /></Link></article><article className="rounded-[28px] border border-white/10 bg-[#10131e] p-6"><Mail className="h-5 w-5 text-orange-300" /><p className="mt-4 text-[10px] font-black tracking-[.18em] text-slate-500">EMAIL SUPPORT</p><a href={`mailto:${supportEmail}`} className="mt-2 inline-block break-all text-base font-black text-orange-200 hover:text-orange-100">{supportEmail}</a><p className="mt-3 text-sm leading-6 text-slate-400">You can also continue your question through our existing WhatsApp support channel.</p><a href={whatsappUrl} target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex min-h-11 items-center gap-2 text-sm font-bold text-emerald-200">Open WhatsApp <ExternalLink className="h-4 w-4" /></a></article></aside>
    </section>

    <section className="relative mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="rounded-[30px] border border-white/10 bg-[#0d0f17] p-6 sm:p-8"><p className="text-[10px] font-black tracking-[.18em] text-orange-300">HOW SUPPORT WORKS</p><h2 className="mt-3 text-3xl font-black">A clearer path to the right request.</h2><div className="mt-7 grid gap-4 md:grid-cols-4">{["Choose the right support category", "Share the relevant details", "Submit your request", "Track or continue the conversation where supported"].map((text, i) => <div key={text} className="relative border-l border-orange-300/30 pl-4 md:border-l-0 md:border-t md:pl-0 md:pt-4"><span className="text-xs font-black text-orange-300">0{i + 1}</span><p className="mt-2 text-sm font-bold leading-6 text-slate-200">{text}</p></div>)}</div></div></section>
    <section className="relative mx-auto grid max-w-7xl gap-5 px-4 py-12 sm:px-6 lg:grid-cols-2 lg:px-8"><article className="rounded-[30px] border border-white/10 bg-[#10131e] p-6"><p className="text-[10px] font-black tracking-[.18em] text-orange-300">UI PREVIEW</p><h2 className="mt-3 text-2xl font-black">A helpful conversation starts with context.</h2><div className="mt-6 space-y-4"><div className="ml-auto max-w-[85%] rounded-2xl rounded-br-sm bg-orange-400 px-4 py-3 text-sm font-medium text-black"><span className="mb-1 block text-[10px] font-black">YOU</span>Hi, I need help understanding the status of my order.</div><div className="max-w-[90%] rounded-2xl rounded-bl-sm bg-white/[.07] px-4 py-3 text-sm leading-6 text-slate-200"><span className="mb-1 block text-[10px] font-black text-orange-200">SOCIALRUSH SUPPORT</span>Thanks. Please make sure the Order ID in your request is correct so the relevant order can be reviewed.</div></div></article><article className="rounded-[30px] border border-white/10 bg-[#10131e] p-6"><p className="text-[10px] font-black tracking-[.18em] text-orange-300">SHARE ONLY WHAT WE NEED</p><h2 className="mt-3 text-2xl font-black">Keep your account safe.</h2><ul className="mt-6 space-y-3 text-sm leading-6 text-slate-300">{["Use the correct Order ID where relevant.", "Provide the public destination link if required.", "Never send social-media passwords.", "Do not send OTPs. Use official SocialRUSH contact channels."].map((item) => <li key={item} className="flex gap-3"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0 text-orange-300" />{item}</li>)}</ul><Link href="/trust" className="mt-6 inline-flex text-sm font-bold text-orange-200 hover:text-orange-100">Read customer safety guidance <ArrowRight className="ml-2 h-4 w-4" /></Link></article></section>
    <section className="relative mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-[10px] font-black tracking-[.18em] text-orange-300">SELF-SERVICE</p><h2 className="mt-3 text-3xl font-black">Find an answer before contacting support.</h2><p className="mt-4 text-sm leading-7 text-slate-400">Browse current services and pricing, or use the dashboard to review existing orders.</p><div className="mt-5 flex flex-wrap gap-2">{[["Services","/services"],["Pricing","/pricing"],["FAQ","/faq"],["Dashboard","/dashboard"]].map(([label, href]) => <Link key={label} href={href} className="rounded-lg border border-white/10 bg-white/[.04] px-3 py-2 text-xs font-bold text-slate-200 hover:border-orange-300/50">{label}</Link>)}</div></div><div className="space-y-2">{faqs.map(([question, answer], index) => <div key={question} className="rounded-2xl border border-white/10 bg-[#10131e]"><button type="button" aria-expanded={openFaq === index} aria-controls={`${formId}-${index}`} onClick={() => setOpenFaq(openFaq === index ? -1 : index)} className="flex min-h-14 w-full items-center justify-between gap-4 px-5 text-left text-sm font-bold"><span>{question}</span><ChevronDown className={`h-4 w-4 shrink-0 text-orange-300 transition ${openFaq === index ? "rotate-180" : ""}`} /></button><AnimatePresence initial={false}>{openFaq === index && <motion.div id={`${formId}-${index}`} initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} className="overflow-hidden"><p className="border-t border-white/[.07] px-5 py-4 text-sm leading-6 text-slate-400">{answer}</p></motion.div>}</AnimatePresence></div>)}</div></div></section>
    <section className="relative mx-auto max-w-7xl px-4 pt-12 sm:px-6 lg:px-8"><div className="rounded-[32px] border border-orange-300/25 bg-[radial-gradient(circle_at_80%_0%,rgba(255,122,0,.26),transparent_38%),#10131e] px-6 py-10 sm:px-10"><p className="text-[10px] font-black tracking-[.18em] text-orange-200">SOCIALRUSH SUPPORT</p><h2 className="mt-3 text-3xl font-black">Still need help?</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-300">Send us the details and use the appropriate SocialRUSH support channel for your request.</p><div className="mt-6 flex flex-wrap gap-3"><a href="#support-form" className="inline-flex min-h-12 items-center rounded-xl bg-white px-5 text-sm font-black text-black">Send a Message</a><a href="#support-form" className="inline-flex min-h-12 items-center rounded-xl border border-white/25 px-5 text-sm font-bold">View FAQ</a><a href={whatsappUrl} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-12 items-center gap-2 rounded-xl border border-emerald-300/30 bg-emerald-400/10 px-5 text-sm font-bold text-emerald-100"><MessageCircle className="h-4 w-4" />WhatsApp Us</a></div></div></section>
  </main></BlogShell>;
}

function SupportPreview() { return <div className="relative mx-auto w-full max-w-xl rounded-[30px] border border-white/15 bg-[#10131e]/90 p-3 shadow-[0_28px_70px_-26px_rgba(0,0,0,.9)] backdrop-blur"><div className="rounded-[22px] border border-white/[.08] bg-[#090b12] p-5"><div className="flex items-center justify-between"><div><p className="text-[10px] font-black tracking-[.16em] text-orange-300">SOCIALRUSH SUPPORT CENTER</p><p className="mt-1 text-lg font-black">Support Request</p></div><span className="rounded-full border border-white/10 px-2 py-1 text-[9px] font-black text-slate-400">UI PREVIEW</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><PreviewField label="Category" value="Order Support" /><PreviewField label="Order ID" value="SR-••••••" /></div><div className="mt-3 rounded-xl border border-white/[.08] bg-white/[.035] p-3"><p className="text-[10px] font-black text-slate-500">MESSAGE</p><p className="mt-1 text-sm text-slate-200">“I need help with my order.”</p></div><div className="mt-4 flex items-center gap-2 rounded-xl border border-emerald-300/20 bg-emerald-400/10 px-3 py-2 text-xs font-bold text-emerald-100"><span className="h-2 w-2 animate-pulse rounded-full bg-emerald-300" />Status: Open</div><div className="mt-5 border-l border-orange-300/40 pl-4 text-xs"><p className="font-bold">Request Submitted</p><p className="my-2 text-orange-300">↓</p><p className="font-bold text-slate-300">Under Review</p><p className="my-2 text-orange-300">↓</p><p className="font-bold text-slate-500">Response</p></div></div><div className="absolute -right-2 top-12 hidden rounded-xl border border-white/10 bg-[#171b2a] px-3 py-2 text-[10px] font-bold text-orange-100 shadow-xl sm:block">Order Found</div><div className="absolute -left-5 bottom-10 hidden rounded-xl border border-white/10 bg-[#171b2a] px-3 py-2 text-[10px] font-bold text-slate-200 shadow-xl sm:block">Ticket Status</div></div> }
function PreviewField({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-white/[.08] bg-white/[.035] p-3"><p className="text-[10px] font-black text-slate-500">{label.toUpperCase()}</p><p className="mt-1 text-sm font-bold text-slate-200">{value}</p></div>; }
function OrderPreview() { return <div className="mt-5 rounded-2xl border border-white/[.08] bg-[#090b12] p-4"><div className="flex items-center justify-between"><span className="text-[10px] font-black tracking-[.15em] text-slate-500">UI PREVIEW</span><span className="rounded-full bg-orange-400/10 px-2 py-1 text-[9px] font-black text-orange-200">IN PROGRESS</span></div><dl className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-slate-500">Order</dt><dd className="mt-1 font-bold">#SR••••••</dd></div><div><dt className="text-slate-500">Platform</dt><dd className="mt-1 font-bold">Instagram</dd></div><div><dt className="text-slate-500">Service</dt><dd className="mt-1 font-bold">Followers</dd></div><div><dt className="text-slate-500">Support</dt><dd className="mt-1 font-bold text-orange-200">View Request</dd></div></dl></div>; }
