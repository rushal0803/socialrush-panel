"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import { ArrowRight, Check, ChevronDown,  LockKeyhole, Minus, Plus, ShieldCheck } from "lucide-react";
import { FaLinkedinIn as LinkedIn } from "react-icons/fa6";
import PublicShell from "@/components/marketing/PublicShell";
import { formatCurrency } from "@/lib/currency";
import { usePreferredCurrency } from "@/lib/currency/use-currency";
import { linkedInUsaCustomCommentsFaqs } from "@/lib/linkedin-usa-custom-comments-content";
import { linkRules, validateCampaignLink } from "@/lib/order-service-experience";
import { calculateServiceTotal } from "@/lib/service-pricing";
import type { SmmService } from "@/lib/smm-service-catalog";

const number = (value: number) => value.toLocaleString("en-IN");

export default function LinkedInUsaCustomCommentsLanding({ service }: { service: SmmService }) {
  const { currency, rates } = usePreferredCurrency("INR");
  const [quantity, setQuantity] = useState(service.minQuantity);
  const [link, setLink] = useState("");
  const [comments, setComments] = useState("");
  const [attempted, setAttempted] = useState(false);
  const rule = linkRules[service.code];
  const lines = useMemo(() => comments.split(/\r?\n/).map((line) => line.trim()).filter(Boolean), [comments]);
  const linkError = link.trim() ? validateCampaignLink(link, rule) : "";
  const quantityValid = Number.isSafeInteger(quantity) && quantity >= service.minQuantity && quantity <= service.maxQuantity;
  const commentsError = !lines.length ? "Enter one custom comment per line." : lines.length !== quantity ? `Add exactly ${number(quantity)} valid comment lines to match your order.` : "";
  const valid = quantityValid && Boolean(link.trim()) && !linkError && !commentsError;
  const updateQuantity = (value: number) => setQuantity(Math.max(service.minQuantity, Math.min(service.maxQuantity, Math.round(value))));
  const presets = [service.minQuantity, 250, 500, 1000, 2500, 5000, service.maxQuantity].filter((value, index, all) => value >= service.minQuantity && value <= service.maxQuantity && all.indexOf(value) === index);
  const rate = formatCurrency(service.pricePer1000, currency, rates);
  const total = formatCurrency(calculateServiceTotal(service.code, quantity), currency, rates);
  const href = `/dashboard/new-order?${new URLSearchParams({ platform: "linkedin", service: service.code, resume: "1", quantity: String(quantity), link: link.trim(), comments })}`;
  const faqs = linkedInUsaCustomCommentsFaqs(service);

  return <PublicShell tone="light3d"><main className="overflow-hidden bg-[#08111a] text-white">
    <section className="relative border-b border-white/10 px-4 pb-16 pt-7 sm:px-6 lg:px-8 lg:pb-24"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_83%_18%,rgba(10,102,194,.3),transparent_27%),radial-gradient(circle_at_13%_8%,rgba(232,140,40,.12),transparent_24%)]" /><div className="relative mx-auto max-w-7xl"><nav aria-label="Breadcrumb" className="mb-10 flex gap-2 text-xs font-bold text-slate-400"><Link href="/">Home</Link><span>/</span><Link href="/services">Services</Link><span>/</span><span className="text-slate-200">LinkedIn USA Custom Comments</span></nav><div className="grid items-center gap-10 lg:grid-cols-[.9fr_1.1fr]"><div><p className="inline-flex items-center gap-2 border-l-2 border-[#0a66c2] pl-3 text-[10px] font-black tracking-[.18em] text-sky-200"><LinkedIn className="h-4 w-4" />LINKEDIN USA CUSTOM COMMENTS</p><h1 className="mt-6 max-w-3xl text-4xl font-black leading-[1.02] tracking-[-.055em] sm:text-5xl lg:text-6xl">Buy LinkedIn USA <span className="text-[#72b7f2]">Custom Comments</span></h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-300 sm:text-lg">Provide the comment text for an eligible public LinkedIn post. Each line is one custom comment; the line count must match your order quantity.</p><div className="mt-8 flex flex-col gap-3 min-[420px]:flex-row"><a href="#order" className="inline-flex min-h-12 items-center justify-center gap-2 bg-[#ff8a1f] px-6 text-sm font-black text-[#15100b]">Build your order <ArrowRight className="h-4 w-4" /></a><Link href="/services" className="inline-flex min-h-12 items-center justify-center border border-white/15 px-5 text-sm font-black">Browse services</Link></div><p className="mt-6 flex items-center gap-2 text-xs font-bold text-slate-300"><ShieldCheck className="h-4 w-4 text-[#ff9d42]" />Public post link only · no LinkedIn password required</p></div><CommentThread rate={rate} delivery={service.deliveryTime} /></div></div></section>
    <section id="order" className="scroll-mt-24 bg-[#0d1722] px-4 py-14 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-[minmax(0,1fr)_390px]"><article className="border border-[#2a5d89] bg-[#111e2c] p-6 sm:p-8"><p className="text-[10px] font-black uppercase tracking-[.18em] text-[#ffad5e]">Service configuration</p><h2 className="mt-3 text-2xl font-black sm:text-3xl">Write the conversation you want to place.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">Use one nonblank line for each comment. This landing page carries your post link and text into the existing secure order flow for final review.</p><div className="mt-7 grid grid-cols-2 gap-2 sm:grid-cols-4">{presets.map(value => <button type="button" key={value} onClick={() => updateQuantity(value)} className={`min-h-[64px] border p-3 text-left text-sm ${quantity === value ? "border-[#58a8e8] bg-[#0a66c2]/20" : "border-white/10 bg-white/[.025]"}`}><b>{number(value)}</b><span className="mt-1 block text-[10px] uppercase text-slate-400">Comments</span></button>)}</div><label className="mt-7 block text-sm font-black">1. Quantity<span className="mt-2 flex border border-white/15 bg-black/20 focus-within:border-[#58a8e8]"><button type="button" aria-label="Decrease quantity" onClick={() => updateQuantity(quantity - 1)} className="grid min-h-14 w-14 place-items-center text-[#ffad5e]"><Minus className="h-4 w-4" /></button><input aria-label="Comment quantity" inputMode="numeric" value={quantity || ""} onChange={event => setQuantity(event.target.value ? Number(event.target.value.replace(/\D/g, "")) : 0)} className="min-w-0 flex-1 bg-transparent text-center outline-none"/><button type="button" aria-label="Increase quantity" onClick={() => updateQuantity(quantity + 1)} className="grid min-h-14 w-14 place-items-center text-[#ffad5e]"><Plus className="h-4 w-4" /></button></span></label><p className={`mt-2 text-xs ${attempted && !quantityValid ? "font-bold text-red-300" : "text-slate-400"}`}>{attempted && !quantityValid ? `Enter ${number(service.minQuantity)}–${number(service.maxQuantity)} comments.` : `Current catalog limits: ${number(service.minQuantity)}–${number(service.maxQuantity)}`}</p><label className="mt-7 block text-sm font-black">2. {rule.label}<span className={`mt-2 flex border bg-black/20 focus-within:border-[#58a8e8] ${attempted && (!link.trim() || linkError) ? "border-red-400" : "border-white/15"}`}><LinkedIn className="m-4 h-5 w-5 shrink-0 text-[#58a8e8]"/><input value={link} onChange={event => setLink(event.target.value)} placeholder={rule.placeholder} className="min-w-0 flex-1 bg-transparent pr-4 outline-none"/></span></label><p className={`mt-2 text-xs leading-6 ${attempted && (!link.trim() || linkError) ? "font-bold text-red-300" : "text-slate-400"}`}>{link.trim() && !linkError ? "Valid public LinkedIn post URL" : attempted && (!link.trim() || linkError) ? linkError || "Enter a valid public LinkedIn post URL." : rule.helper}</p><label className="mt-7 block text-sm font-black">3. Custom comment text<textarea value={comments} onChange={event => setComments(event.target.value)} rows={7} placeholder={"A thoughtful response to the post.\nA second comment written for this post."} className={`mt-2 w-full resize-y border bg-black/20 px-4 py-3 text-sm leading-6 outline-none focus:border-[#58a8e8] ${attempted && commentsError ? "border-red-400" : "border-white/15"}`}/></label><p className={`mt-2 text-xs leading-6 ${attempted && commentsError ? "font-bold text-red-300" : "text-slate-400"}`}>{attempted && commentsError ? commentsError : `${number(lines.length)} / ${number(quantity)} valid comment lines · one comment per line.`}</p></article><aside className="border border-[#ff9d42]/35 bg-[#141616] p-6 lg:sticky lg:top-28 lg:h-fit"><p className="text-[10px] font-black uppercase tracking-[.18em] text-[#ffad5e]">Live order details</p><h3 className="mt-2 text-xl font-black">{service.name}</h3><dl className="mt-6 space-y-3 text-sm">{[["Quantity",number(quantity)],["Catalog rate",`${rate} / 1K`],["Delivery estimate",service.deliveryTime],["Refill / support",service.refillPolicy],["Post URL",link.trim() && !linkError ? "Ready" : "Required"],["Comment text",lines.length === quantity ? `${number(lines.length)} lines ready` : `${number(lines.length)} / ${number(quantity)} lines`]].map(([label,value]) => <div key={label} className="flex justify-between gap-4"><dt className="text-slate-400">{label}</dt><dd className="max-w-[58%] text-right font-black">{value}</dd></div>)}</dl><div className="mt-6 border-y border-white/10 py-4"><p className="text-[10px] font-black uppercase text-slate-400">Calculated total</p><p className="mt-2 text-3xl font-black">{total}</p></div><Link href={valid ? href : "#order"} onClick={event => { if (!valid) { event.preventDefault(); setAttempted(true); } }} aria-disabled={!valid} className={`mt-6 flex min-h-14 items-center justify-center gap-2 text-sm font-black ${valid ? "bg-[#ff8a1f] text-[#17110b]" : "bg-white/10 text-slate-400"}`}>Continue to Secure Order <ArrowRight className="h-4 w-4"/></Link><p className="mt-3 text-center text-[11px] text-slate-400">Secure checkout · dashboard tracking</p></aside></div></section>
    <section className="px-4 py-16 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-10 lg:grid-cols-[.72fr_1.28fr] lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.16em] text-[#ffad5e]">Link eligibility</p><h2 className="mt-4 max-w-md text-3xl font-black leading-tight">Use the exact public post link.</h2><p className="mt-5 max-w-sm text-sm leading-7 text-slate-300">The post URL identifies where your supplied comment text belongs. Profile, company, and group links describe different LinkedIn destinations.</p></div><div className="border-y border-[#0a66c2]/45 py-5"><p className="flex flex-wrap items-center gap-3 text-sm"><Check className="h-5 w-5 text-emerald-300"/><b>Correct:</b><code className="break-all bg-white/[.06] px-2 py-1 text-sky-100">linkedin.com/posts/... or linkedin.com/feed/update/...</code></p><div className="mt-5 grid gap-3 text-sm text-slate-400 sm:grid-cols-3"><p><b className="block text-white">Wrong</b>linkedin.com/in/...</p><p><b className="block text-white">Wrong</b>company page URL</p><p><b className="block text-white">Wrong</b>group URL</p></div></div></div></section>
    <section className="border-y border-white/10 bg-[#0d1722] px-4 py-16 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-10 lg:grid-cols-[.72fr_1.28fr]"><div><p className="text-xs font-black uppercase tracking-[.16em] text-[#ffad5e]">From text to order</p><h2 className="mt-4 text-3xl font-black">A short, deliberate workflow.</h2></div><ol className="border-l border-[#0a66c2] pl-5 text-sm sm:grid sm:grid-cols-2 sm:gap-x-10 sm:gap-y-7">{["Choose quantity","Paste an eligible public post URL","Add one custom comment per line","Review the live total","Continue securely","Track the order in your dashboard"].map((step,index) => <li key={step} className="relative pb-6 last:pb-0 sm:pb-0"><b className="text-[#ffad5e]">0{index + 1}</b><p className="mt-1 font-bold text-slate-200">{step}</p></li>)}</ol></div></section>
    <section className="px-4 py-16 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-8 lg:grid-cols-[.72fr_1.28fr]"><div><LockKeyhole className="h-7 w-7 text-[#ffad5e]"/><h2 className="mt-4 text-3xl font-black">Your text. One public post. No credentials.</h2></div><div className="text-sm leading-7 text-slate-300"><p>Custom comments means you supply the text, one line per comment, for the eligible LinkedIn post you submit. It does not mean profile comments, automatic replies, or a guarantee of discussion.</p><p className="mt-5 border-l-2 border-[#ff8a1f] pl-4 font-bold text-white">SocialRUSH never asks for your LinkedIn password, email password, OTP, or recovery code.</p></div></div></section>
    <section className="border-y border-white/10 bg-[#0d1722] px-4 py-16 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-[#ffad5e]">Choose the right LinkedIn service</p><div className="mt-6 divide-y divide-white/10 border-y border-white/10">{[["LinkedIn USA Custom Comments","User-defined comments on a submitted eligible public LinkedIn post.","/services/linkedin-usa-custom-comments"],["LinkedIn USA Post Likes","Like activity on a submitted eligible public LinkedIn post.","/services/linkedin-usa-post-likes"],["LinkedIn USA Followers","Follower-focused service.","/services/linkedin-usa-followers"],["LinkedIn USA Connections","Connection-focused service.","/services/linkedin-usa-connections"],["LinkedIn USA Endorsements","Skill/profile endorsement service.","/services/linkedin-usa-endorsements"],["LinkedIn USA Group Members","Group membership service.","/services/linkedin-usa-group-members"]].map(([name,text,url]) => <Link key={name} href={url} className="group flex items-center justify-between gap-5 py-5"><span><b className="block">{name}</b><span className="mt-1 block text-sm text-slate-400">{text}</span></span><ArrowRight className="h-5 w-5 shrink-0 text-[#79b9ed] transition group-hover:translate-x-1"/></Link>)}</div><div className="mt-8 flex flex-wrap gap-3 text-sm font-bold"><Link href="/linkedin-followers" className="border border-white/15 px-4 py-3">LinkedIn followers</Link><Link href="/linkedin-growth-india" className="border border-white/15 px-4 py-3">LinkedIn growth hub</Link><Link href="/pricing" className="border border-white/15 px-4 py-3">Pricing</Link><Link href="/services" className="border border-white/15 px-4 py-3">All services</Link></div></div></section>
    <section className="px-4 py-16 sm:px-6 lg:px-8"><div className="mx-auto max-w-4xl"><p className="text-xs font-black uppercase tracking-[.16em] text-[#ffad5e]">Helpful answers</p><h2 className="mt-3 text-3xl font-black">LinkedIn USA Custom Comments FAQ</h2><div className="mt-8 divide-y divide-white/10 border-y border-white/10">{faqs.map(([question,answer],index) => <details key={question} open={index === 0} className="group py-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 text-sm font-black"><span>{question}</span><ChevronDown className="h-5 w-5 shrink-0 text-[#ffad5e] transition group-open:rotate-180"/></summary><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-300">{answer}</p></details>)}</div></div></section>
  </main></PublicShell>;
}

function CommentThread({ rate, delivery }: { rate: string; delivery: string }) { return <aside aria-label="Illustrative LinkedIn post and custom comment thread" className="relative border border-[#3473a8] bg-[#101a26] p-4 sm:-mt-5 sm:p-5"><div className="absolute right-0 top-0 h-16 w-16 border-b border-l border-[#3473a8]/50"/><div className="flex items-center justify-between text-[10px] font-black uppercase tracking-[.14em]"><span className="text-[#79b9ed]">Public post / illustrative</span><span className="border border-[#3b6384] px-2 py-1 text-[#b9d9f4]">USA service</span></div><div className="mt-4 bg-[#f8fafc] p-4 text-slate-900 sm:p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-[#0a66c2] text-white"><LinkedIn className="h-4 w-4"/></span><span className="text-xs"><b className="block">Public LinkedIn post</b><small className="block text-[10px] text-slate-500">Generic interface example</small></span></div><div className="mt-5 space-y-2"><i className="block h-2 w-[88%] bg-slate-200"/><i className="block h-2 w-[70%] bg-slate-200"/></div><div className="mt-5 border-t border-slate-200 pt-4"><p className="text-[10px] font-black uppercase tracking-wide text-slate-500">Comment thread</p>{["Custom comment text · illustrative","Another supplied comment · illustrative"].map((text,index) => <div key={text} className="mt-3 flex gap-2"><span className={`h-7 w-7 shrink-0 rounded-full ${index ? "bg-slate-300" : "bg-sky-200"}`}/><p className="min-w-0 border-l-2 border-[#0a66c2] bg-slate-100 px-3 py-2 text-[11px] font-medium leading-4">{text}</p></div>)}</div></div><div className="mt-4 flex items-end justify-between gap-4 border-l-2 border-[#ff8a1f] pl-3"><div className="text-xs"><p className="font-black text-white">Live service details</p><p className="mt-1 text-slate-400">Post → comment thread → your text · {delivery}</p></div><p className="text-right text-sm font-black text-[#ffbd79]">{rate}<small className="block text-[10px] font-bold text-slate-400">per 1K</small></p></div><p className="mt-4 text-[10px] leading-5 text-slate-500">Generic interface treatment only. It does not represent a person, account, customer data, or actual results.</p></aside>; }
