import Link from "next/link";
import PublicShell from "@/components/marketing/PublicShell";
import PageHero from "@/components/marketing/PageHero";
import { agencyServices } from "@/lib/marketing/content";

export default function ServicesPage() {
  return <PublicShell><PageHero eyebrow="Agency services" title="Connected digital services built around business outcomes." description="From search and advertising to websites and automation, SocialRUSH brings the right capabilities together in one clear delivery plan."/><section className="px-5 py-20 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-7xl gap-6 md:grid-cols-2">{agencyServices.map((service,index) => <article id={service.slug} key={service.slug} className="scroll-mt-28 rounded-3xl border border-slate-200 bg-white p-7 shadow-sm transition hover:-translate-y-1 hover:border-blue-200 hover:shadow-xl"><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-blue-50 text-sm font-black text-blue-600">{String(index+1).padStart(2,"0")}</span><span className="rounded-full bg-slate-50 px-3 py-1.5 text-[9px] font-bold uppercase tracking-wider text-slate-500">Agency service</span></div><h2 className="mt-6 text-xl font-bold text-[#07152f]">{service.name}</h2><p className="mt-3 text-sm leading-7 text-slate-500">{service.summary}</p><div className="mt-6 grid gap-5 sm:grid-cols-2"><div><h3 className="text-[10px] font-bold uppercase tracking-wider text-blue-600">Typical deliverables</h3><ul className="mt-3 space-y-2">{service.deliverables.map((item) => <li key={item} className="flex gap-2 text-xs text-slate-600"><span className="text-emerald-500">✓</span>{item}</li>)}</ul></div><div><h3 className="text-[10px] font-bold uppercase tracking-wider text-blue-600">Ideal for</h3><p className="mt-3 text-xs leading-6 text-slate-600">{service.ideal}</p></div></div><Link href={`/contact?service=${service.slug}`} className="mt-7 inline-flex rounded-xl bg-[#07152f] px-4 py-2.5 text-xs font-bold text-white transition hover:bg-blue-600">Request a proposal →</Link></article>)}</div></section></PublicShell>;
}
