import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import { closeTicket, replyToTicket } from "@/app/admin/actions";
import { AdminPageHeader, AdminStatus, primaryButton } from "@/components/admin/AdminUI";

export default async function AdminSupportPage({ searchParams }: { searchParams?: { ticket?: string } }) {
  const supabase = await createClient();
  const { data: tickets } = await supabase.from("support_tickets").select("id, subject, status, created_at, user_id, profiles(email, full_name)").order("created_at", { ascending: false });
  const selectedId = searchParams?.ticket || tickets?.[0]?.id;
  const selected = tickets?.find((ticket) => ticket.id === selectedId);
  const { data: messages } = selectedId ? await supabase.from("support_messages").select("id, sender_id, message, created_at, profiles(full_name, role)").eq("ticket_id", selectedId).order("created_at") : { data: [] };
  return <main className="mx-auto max-w-[1500px] p-5 sm:p-8"><AdminPageHeader title="Support tickets" description="Review customer issues, reply to conversations, and close resolved tickets."/><div className="mt-7 grid gap-6 xl:grid-cols-[380px_1fr]"><section className="panel-card overflow-hidden"><div className="border-b border-slate-100 p-5"><h2 className="text-sm font-bold">All tickets</h2><p className="mt-1 text-xs text-slate-400">{tickets?.length ?? 0} conversations</p></div><div className="max-h-[680px] divide-y divide-slate-100 overflow-y-auto">{(tickets ?? []).map((ticket) => { const profile = ticket.profiles as unknown as { full_name?: string; email?: string } | null; return <Link key={ticket.id} href={`/admin/support?ticket=${ticket.id}`} className={`block p-5 transition ${selectedId === ticket.id ? "bg-blue-50" : "hover:bg-slate-50"}`}><div className="flex items-center justify-between"><span className="text-[10px] font-bold text-blue-600">#{ticket.id.slice(0,8).toUpperCase()}</span><AdminStatus value={ticket.status}/></div><p className="mt-3 truncate text-xs font-semibold text-slate-700">{ticket.subject}</p><p className="mt-2 truncate text-[10px] text-slate-400">{profile?.full_name || profile?.email || "User"} · {new Date(ticket.created_at).toLocaleDateString("en-IN")}</p></Link>; })}{!tickets?.length && <p className="p-10 text-center text-xs text-slate-400">No support tickets found.</p>}</div></section><section className="panel-card flex min-h-[620px] flex-col overflow-hidden">{selected ? <><div className="flex flex-col gap-3 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><span className="text-[10px] font-bold text-blue-600">#{selected.id.slice(0,8).toUpperCase()}</span><AdminStatus value={selected.status}/></div><h2 className="mt-2 text-sm font-bold">{selected.subject}</h2></div>{selected.status !== "closed" && <form action={closeTicket}><input type="hidden" name="ticket_id" value={selected.id}/><button className="rounded-xl border border-slate-200 px-4 py-2.5 text-[10px] font-bold text-slate-600 hover:bg-slate-50">Close ticket</button></form>}</div><div className="flex-1 space-y-4 bg-slate-50/60 p-5 sm:p-7">{(messages ?? []).map((message) => { const sender = message.profiles as unknown as { full_name?: string; role?: string } | null; const admin = sender?.role === "admin"; return <div key={message.id} className={`flex max-w-2xl gap-3 ${admin ? "ml-auto flex-row-reverse" : ""}`}><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl text-[9px] font-bold text-white ${admin ? "bg-blue-600" : "bg-[#0a1b3d]"}`}>{admin ? "ADM" : "USR"}</span><div><div className={`rounded-2xl p-4 text-xs leading-6 shadow-sm ${admin ? "rounded-tr-sm bg-blue-600 text-white" : "rounded-tl-sm bg-white text-slate-600"}`}>{message.message}</div><p className={`mt-2 text-[9px] text-slate-400 ${admin ? "text-right" : ""}`}>{sender?.full_name || (admin ? "Administrator" : "Customer")} · {new Date(message.created_at).toLocaleString("en-IN")}</p></div></div>; })}{!messages?.length && <p className="text-center text-xs text-slate-400">No messages in this conversation.</p>}</div>{selected.status !== "closed" && <form action={replyToTicket} className="border-t border-slate-100 bg-white p-4"><input type="hidden" name="ticket_id" value={selected.id}/><div className="flex gap-3"><textarea name="message" required className="min-h-14 flex-1 resize-none rounded-xl border border-slate-200 px-4 py-3 text-xs outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10" placeholder="Write an admin reply..."/><button className={`${primaryButton} self-end`}>Send reply</button></div></form>}</> : <div className="grid flex-1 place-items-center p-8 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-blue-50 text-blue-600">?</span><p className="mt-4 text-sm font-semibold">No ticket selected</p><p className="mt-2 text-xs text-slate-400">Choose a ticket to view its conversation.</p></div></div>}</section></div></main>;
}
