import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import { updateOrder } from "@/app/admin/actions";
import { AdminPageHeader, AdminStatus, inputClass, primaryButton } from "@/components/admin/AdminUI";

const statuses = ["all","pending","processing","in_progress","completed","partial","cancelled","refunded","failed"];
const money = (value: number | string) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR" }).format(Number(value));

export default async function AdminOrdersPage({ searchParams }: { searchParams?: { status?: string } }) {
  const supabase = await createClient(); const filter = statuses.includes(searchParams?.status || "") ? searchParams!.status! : "all";
  let query = supabase.from("orders").select("id, link, quantity, package_name, charge, status, provider_order_id, admin_notes, created_at, updated_at, profiles(email, full_name), services(name, delivery_time)").order("created_at", { ascending: false }); if (filter !== "all") query = query.eq("status", filter); const { data: orders } = await query.limit(150);
  return <main className="mx-auto max-w-[1700px] p-5 sm:p-8"><AdminPageHeader title="Order operations" description="Manage fulfillment, provider references, internal notes, and customer delivery status."/><div className="mt-6 flex flex-wrap gap-2">{statuses.map((item) => <Link key={item} href={item === "all" ? "/admin/orders" : `/admin/orders?status=${item}`} className={`rounded-xl px-3.5 py-2.5 text-xs font-semibold capitalize ${filter === item ? "bg-blue-600 text-white" : "border bg-white text-slate-500"}`}>{item.replaceAll("_"," ")}</Link>)}</div><section className="panel-card mt-5 overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[1250px] text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase text-slate-400"><tr>{["Order","Customer","Service","Quantity","Amount","Status","Provider ID","Updated","Manage"].map((head) => <th key={head} className="px-5 py-3">{head}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{(orders ?? []).map((order) => { const profile = order.profiles as unknown as { full_name?: string; email?: string } | null; const service = order.services as unknown as { name?: string; delivery_time?: string } | null; return <tr key={order.id} className="align-top hover:bg-slate-50"><td className="px-5 py-4"><p className="font-bold text-blue-600">#{order.id.slice(0,8).toUpperCase()}</p><p className="mt-1 text-[9px] text-slate-400">{new Date(order.created_at).toLocaleDateString("en-IN")}</p></td><td className="px-5 py-4"><p className="font-semibold">{profile?.full_name || "Customer"}</p><p className="mt-1 text-[9px] text-slate-400">{profile?.email}</p></td><td className="max-w-[230px] px-5 py-4"><p className="truncate font-semibold">{service?.name || "Service"}</p><p className="mt-1 text-[9px] text-slate-400">{service?.delivery_time || "1-7 days"} delivery</p></td><td className="px-5 py-4">{Number(order.quantity).toLocaleString()}</td><td className="px-5 py-4 font-bold">{money(order.charge)}</td><td className="px-5 py-4"><AdminStatus value={order.status}/></td><td className="px-5 py-4 text-slate-500">{order.provider_order_id || "-"}</td><td className="px-5 py-4 text-[10px] text-slate-400">{new Date(order.updated_at || order.created_at).toLocaleString("en-IN")}</td><td className="px-5 py-4"><details><summary className="cursor-pointer list-none rounded-lg bg-blue-50 px-3 py-2 text-center text-[10px] font-bold text-blue-600">Update</summary><form action={updateOrder} className="mt-3 w-72 space-y-3 rounded-xl border bg-white p-4 shadow-xl"><input type="hidden" name="id" value={order.id}/><label className="block text-[9px] font-bold uppercase text-slate-400">Status<select name="status" defaultValue={order.status} className={inputClass}>{statuses.slice(1).map((item) => <option key={item} value={item}>{item.replaceAll("_"," ")}</option>)}</select></label><label className="block text-[9px] font-bold uppercase text-slate-400">Provider order ID<input name="provider_order_id" defaultValue={order.provider_order_id || ""} className={inputClass} placeholder="Provider reference"/></label><label className="block text-[9px] font-bold uppercase text-slate-400">Admin notes<textarea name="admin_notes" defaultValue={order.admin_notes || ""} className={`${inputClass} min-h-20 resize-none`} placeholder="Internal notes only"/></label><button className={`${primaryButton} w-full`}>Save order update</button></form></details></td></tr>; })}{!orders?.length && <tr><td colSpan={9} className="p-14 text-center text-slate-400">No orders match this status.</td></tr>}</tbody></table></div></section></main>;
}
