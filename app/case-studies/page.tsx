/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";
import { ArrowRight, FileCheck2, ShieldCheck, Sparkles } from "lucide-react";
import PublicShell from "@/components/marketing/PublicShell";
import { createAdminClient } from "@/lib/supabase/admin";
import { createPageMetadata } from "@/lib/seo/metadata";

export const metadata = createPageMetadata({ title: "Verified Customer Case Studies", description: "Permission-backed SocialRUSH customer campaign stories with factual order context.", path: "/case-studies" });
export const dynamic = "force-dynamic";

export default async function CaseStudies() {
  const { data = [] } = await createAdminClient().from("case_studies").select("slug,title,platform,service_name,customer_type,challenge,outcome,delivery_timeline").eq("published", true).eq("permission_confirmed", true).order("featured", { ascending: false }).order("published_at", { ascending: false });
  return <PublicShell>
    <section className="relative overflow-hidden px-4 pb-14 pt-14 sm:px-6 sm:pb-20 sm:pt-20 lg:px-8 lg:pb-24"><div className="pointer-events-none absolute left-1/2 top-0 h-[32rem] w-[70rem] -translate-x-1/2 rounded-full bg-orange-500/[.13] blur-[120px]"/><div className="relative mx-auto grid max-w-7xl gap-8 lg:grid-cols-[1fr_.8fr] lg:items-end"><div><span className="inline-flex items-center gap-2 rounded-full border border-orange-400/30 bg-orange-500/[.1] px-4 py-2 text-[10px] font-black uppercase tracking-[.16em] text-orange-200"><FileCheck2 className="h-4 w-4"/>Permission-backed stories</span><h1 className="mt-5 max-w-3xl text-4xl font-black tracking-[-.055em] text-white sm:text-5xl">Campaign context, presented with care.</h1><p className="mt-5 max-w-2xl text-base leading-8 text-[#A8AFBD]">Read verified customer stories with factual service and delivery context. They are not promises of performance or guaranteed outcomes.</p></div><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl border border-white/[.09] bg-[#101219] p-4"><ShieldCheck className="h-5 w-5 text-emerald-300"/><p className="mt-4 text-sm font-black text-white">Verified context</p><p className="mt-1 text-xs leading-5 text-[#A8AFBD]">Published only with permission.</p></div><div className="rounded-2xl border border-orange-400/25 bg-orange-500/[.07] p-4"><Sparkles className="h-5 w-5 text-orange-300"/><p className="mt-4 text-sm font-black text-white">Clear framing</p><p className="mt-1 text-xs leading-5 text-[#A8AFBD]">No invented figures or claims.</p></div></div></div></section>
    <section className="border-y border-white/[.07] bg-[#0C0E14] px-4 py-14 sm:px-6 lg:px-8 lg:py-20"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-orange-300">Customer stories</p><h2 className="mt-2 text-3xl font-black text-white">Verified case-study library</h2></div><span className="rounded-full border border-white/10 bg-white/[.04] px-3 py-2 text-xs font-bold text-[#A8AFBD]">{data.length} published {data.length === 1 ? "story" : "stories"}</span></div><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.length ? data.map((s: any, index: number) => <article key={s.slug} className="group relative flex min-h-[290px] flex-col overflow-hidden rounded-3xl border border-white/[.09] bg-[#101219] p-6 shadow-[0_18px_46px_rgba(0,0,0,.22)] transition hover:-translate-y-1 hover:border-orange-400/45"><span className="absolute right-5 top-4 text-5xl font-black text-white/[.035]">0{index + 1}</span><div className="relative flex flex-wrap gap-2 text-[10px] font-black uppercase tracking-[.12em]"><span className="rounded-full bg-orange-500/[.12] px-2.5 py-1.5 text-orange-200">{s.platform}</span><span className="rounded-full bg-white/[.06] px-2.5 py-1.5 text-[#A8AFBD]">{s.customer_type}</span></div><h2 className="relative mt-5 text-xl font-black leading-7 text-white">{s.title}</h2><p className="relative mt-3 text-sm leading-6 text-[#A8AFBD]">{s.challenge}</p><div className="relative mt-auto border-t border-white/[.08] pt-4"><p className="text-[10px] font-black uppercase tracking-[.12em] text-emerald-300">Documented outcome</p><p className="mt-1 text-sm font-semibold text-[#D7DBE3]">{s.outcome}</p><Link href={`/case-studies/${s.slug}`} className="mt-4 inline-flex min-h-10 items-center gap-2 text-sm font-black text-orange-300">Read story <ArrowRight className="h-4 w-4 transition group-hover:translate-x-1"/></Link></div></article>) : <div className="col-span-full rounded-3xl border border-dashed border-white/[.15] bg-[#101219] px-6 py-14 text-center"><FileCheck2 className="mx-auto h-7 w-7 text-orange-300"/><h2 className="mt-4 text-xl font-black text-white">No verified stories are published yet.</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#A8AFBD]">Case studies are added only after the relevant customer permission and factual context are available.</p><Link href="/services" className="btn-primary mt-6">Explore services</Link></div>}</div></div></section>
  </PublicShell>;
}
