import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import { PageHeader } from "@/components/dashboard/Ui";
export default async function PortalServicesPage(){const supabase=await createClient();const {data:services}=await supabase.from("services").select("id,name,description,rate,min,max,delivery_time,categories(name)").eq("status","active").order("name");return <main className="mx-auto max-w-[1500px] p-5 sm:p-8"><PageHeader title="Service catalog" description="Browse active growth, advertising, content, and automation services."/><div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{(services??[]).map((service)=><article key={service.id} className="panel-card p-6 transition hover:-translate-y-1 hover:shadow-lg"><p className="text-[10px] font-bold uppercase tracking-wider text-blue-600">{(service.categories as unknown as {name?:string}|null)?.name||"Growth service"}</p><h2 className="mt-3 text-lg font-bold">{service.name}</h2><p className="mt-3 min-h-14 text-xs leading-6 text-slate-500">{service.description}</p><div className="mt-5 flex justify-between rounded-xl bg-slate-50 p-3 text-xs"><span>From ₹{Number(service.rate).toLocaleString("en-IN")}</span><span>{service.delivery_time||"Planned delivery"}</span></div><Link href="/dashboard/new-campaign" className="mt-5 block rounded-xl bg-blue-600 px-4 py-3 text-center text-xs font-bold text-white">Launch campaign</Link></article>)}</div></main>}
