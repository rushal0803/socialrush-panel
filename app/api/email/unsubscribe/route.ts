import { NextRequest, NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { createHmac, timingSafeEqual } from "crypto";
export const runtime="nodejs";
export async function GET(request:NextRequest){const id=request.nextUrl.searchParams.get("u"),token=request.nextUrl.searchParams.get("t"),secret=process.env.EMAIL_UNSUBSCRIBE_SECRET;if(!id||!token||!secret||!/^[0-9a-f-]{36}$/i.test(id))return new NextResponse("Invalid unsubscribe link.",{status:400});const expected=createHmac("sha256",secret).update(id).digest("base64url");if(token.length!==expected.length||!timingSafeEqual(Buffer.from(token),Buffer.from(expected)))return new NextResponse("Invalid unsubscribe link.",{status:400});const db=createAdminClient();const {data:profile}=await db.from("profiles").select("email,notification_preferences").eq("id",id).maybeSingle();if(!profile)return new NextResponse("This unsubscribe link is no longer active.",{status:404});const preferences={...(profile.notification_preferences||{}),marketing:false};await db.from("profiles").update({notification_preferences:preferences}).eq("id",id);if(profile.email)await db.from("crm_suppression_list").upsert({email:String(profile.email).trim().toLowerCase(),reason:"customer_unsubscribe",source:"customer_lifecycle"},{onConflict:"email"});return new NextResponse("You have been unsubscribed from SocialRUSH product updates.",{status:200,headers:{"content-type":"text/plain; charset=utf-8"}});}
