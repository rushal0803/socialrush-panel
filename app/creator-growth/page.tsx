import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import PlatformIcon from "@/components/PlatformIcon";
import PublicShell from "@/components/marketing/PublicShell";
import { activeSmmServices, platformMeta, type SmmPlatformId } from "@/lib/smm-service-catalog";
import { createPageMetadata } from "@/lib/seo/metadata";

export const metadata: Metadata = createPageMetadata({ title: "Creator Growth Hub", description: "Choose SocialRUSH services by platform and campaign goal, with current service options, transparent ordering and dashboard tracking.", path: "/creator-growth" });
const platforms = Object.keys(platformMeta) as SmmPlatformId[];
const goals = [["Build social proof", "followers"], ["Increase engagement", "likes"], ["Grow video visibility", "views"], ["Strengthen creator presence", "followers"]] as const;

export default function CreatorGrowthPage() {
  return <PublicShell><section className="relative overflow-hidden px-5 py-14 sm:px-6 lg:px-8 lg:py-20"><div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_15%_10%,rgba(255,122,0,.2),transparent_28%),linear-gradient(#07080D,#0c0e14)]" /><div className="mx-auto max-w-6xl"><p className="text-xs font-black uppercase tracking-[.17em] text-orange-300">Creator Growth Hub</p><h1 className="mt-4 max-w-3xl text-4xl font-black tracking-[-.05em] sm:text-6xl">Choose a service around your next content goal.</h1><p className="mt-5 max-w-2xl text-base leading-7 text-slate-300">Explore active SocialRUSH options by platform. You will always review the current price, quantity and public destination before ordering.</p></div></section><section className="px-5 pb-16 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><h2 className="text-2xl font-black">What&apos;s your growth goal?</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{goals.map(([title, keyword]) => { const match = activeSmmServices.find((service) => service.code.includes(keyword)); return <Link key={title} href={match ? `/order-summary?service=${match.code}` : "/services"} className="rounded-2xl border border-white/10 bg-white/[.035] p-5 transition hover:border-orange-400/45"><CheckCircle2 className="h-5 w-5 text-orange-300" /><h3 className="mt-4 font-black">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-400">Review an active option and its current ordering details.</p><span className="mt-4 inline-flex items-center gap-1 text-xs font-bold text-orange-200">Explore options <ArrowRight className="h-3.5 w-3.5" /></span></Link>; })}</div><h2 className="mt-14 text-2xl font-black">Choose your platform</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{platforms.map((platform) => { const services = activeSmmServices.filter((service) => service.platform === platform); if (!services.length) return null; return <article key={platform} className="rounded-2xl border border-white/10 bg-[#101219] p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-orange-500/10"><PlatformIcon platform={platformMeta[platform].label} className="h-5 w-5" /></span><h3 className="font-black">Grow {platformMeta[platform].label}</h3></div><div className="mt-4 space-y-2">{services.map((service) => <Link key={service.code} href={`/order-summary?service=${service.code}`} className="flex items-center justify-between rounded-xl bg-white/[.04] px-3 py-2.5 text-sm font-semibold text-slate-200 hover:bg-orange-500/10 hover:text-white"><span>{service.name}</span><ArrowRight className="h-4 w-4 text-orange-300" /></Link>)}</div></article>; })}</div><section className="mt-14 rounded-3xl border border-orange-400/25 bg-orange-500/[.07] p-6 sm:p-8"><p className="text-xs font-black uppercase tracking-[.16em] text-orange-200">Not sure where to start?</p><h2 className="mt-3 text-2xl font-black">Compare services and prepare your content first.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-300">Use a free Creator Tool, then compare current service options. No tool result promises a commercial outcome.</p><div className="mt-5 flex flex-wrap gap-3"><Link href="/tools" className="inline-flex min-h-11 items-center rounded-xl bg-white px-4 text-sm font-bold text-black">Explore Creator Tools</Link><Link href="/services" className="inline-flex min-h-11 items-center rounded-xl border border-orange-400/30 px-4 text-sm font-bold text-orange-100">Browse all services</Link><Link href="/instagram-growth-india" className="inline-flex min-h-11 items-center rounded-xl border border-orange-400/30 px-4 text-sm font-bold text-orange-100">Instagram Growth India</Link><Link href="/youtube-growth-india" className="inline-flex min-h-11 items-center rounded-xl border border-orange-400/30 px-4 text-sm font-bold text-orange-100">YouTube Growth India</Link></div></section></div></section></PublicShell>;
}
