export type EmailTemplate = { subject: string; html: string; text: string };
import { createHmac } from "crypto";

type Order = { id: string; public_order_id?: string | null; platform?: string | null; service_name?: string | null; quantity?: number | null; charge?: number | null; status?: string | null; created_at?: string | null };
type FrameOptions = { preheader: string; headline: string; body: string; card?: string; cta: string; href: string; support: string };

const site = () => (process.env.NEXT_PUBLIC_SITE_URL || "https://www.getsocialrush.com").replace(/\/$/, "");
const esc = (value: unknown) => String(value ?? "—").replace(/[&<>\"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[character] as string));
const first = (name?: string | null) => (name || "there").trim().split(/\s+/)[0] || "there";
const orderId = (order: Order) => order.public_order_id || order.id;
const amount = (value?: number | null) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(Number(value || 0));
const orderDate = (value?: string | null) => { const date = value ? new Date(value) : null; return date && !Number.isNaN(date.getTime()) ? new Intl.DateTimeFormat("en-IN", { day: "numeric", month: "short", year: "numeric" }).format(date) : "—"; };

function frame({ preheader, headline, body, card, cta, href, support }: FrameOptions) {
  const home = site();
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"></head>
<body style="margin:0;padding:0;background:#07080D;color:#F8FAFC;font-family:Arial,Helvetica,sans-serif;-webkit-text-size-adjust:100%;-ms-text-size-adjust:100%;">
<div style="display:none;max-height:0;overflow:hidden;mso-hide:all;font-size:1px;line-height:1px;color:#07080D;opacity:0;">${preheader}</div>
<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;background:#07080D;"><tr><td align="center" style="padding:28px 14px;">
<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;max-width:640px;background:#101219;border:1px solid #252934;border-radius:18px;overflow:hidden;">
<tr><td style="height:4px;line-height:4px;font-size:0;background:#FF7600;background:linear-gradient(135deg,#FF6200 0%,#FF9A00 100%);">&nbsp;</td></tr>
<tr><td style="padding:28px 32px 20px;background:#0C0E14;border-bottom:1px solid #252934;"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0"><tr><td valign="middle"><img src="https://www.getsocialrush.com/images/brand/socialrush-logo-transparent.png" width="174" alt="SocialRUSH" style="display:block;width:174px;max-width:100%;height:auto;border:0;outline:none;text-decoration:none;"></td></tr></table></td></tr>
<tr><td style="padding:34px 32px 10px;"><h1 style="margin:0;color:#F8FAFC;font-size:29px;line-height:37px;font-weight:700;letter-spacing:-.4px;">${headline}</h1></td></tr>
<tr><td style="padding:10px 32px 0;font-size:16px;line-height:26px;color:#A8AFBD;">${body}</td></tr>
${card ? `<tr><td style="padding:26px 32px 0;">${card}</td></tr>` : ""}
<tr><td style="padding:30px 32px 0;"><table role="presentation" cellspacing="0" cellpadding="0" border="0"><tr><td align="center" style="border-radius:9px;background:#FF7600;background:linear-gradient(135deg,#FF6200 0%,#FF9A00 100%);"><a href="${href}" style="display:inline-block;padding:14px 22px;border-radius:9px;color:#FFFFFF;font-size:15px;line-height:20px;font-weight:700;text-decoration:none;">${cta}</a></td></tr></table></td></tr>
<tr><td style="padding:26px 32px 34px;font-size:14px;line-height:23px;color:#A8AFBD;">${support}</td></tr>
<tr><td style="padding:24px 32px;background:#0C0E14;border-top:1px solid #252934;"><p style="margin:0 0 5px;color:#F8FAFC;font-size:14px;line-height:20px;font-weight:700;">SocialRUSH</p><p style="margin:0 0 14px;color:#747B89;font-size:13px;line-height:20px;">Social growth, simplified.</p><p style="margin:0 0 14px;font-size:13px;line-height:20px;"><a href="${home}" style="color:#FF9A2E;text-decoration:none;">getsocialrush.com</a></p><p style="margin:0;color:#747B89;font-size:12px;line-height:18px;">This is an account/service communication from SocialRUSH.</p></td></tr>
</table></td></tr></table></body></html>`;
}

function details(order: Order) {
  const row = (label: string, value: string, last = false) => `<tr><td style="padding:11px 0${last ? "" : ";border-bottom:1px solid #252934"};width:38%;font-size:12px;line-height:18px;color:#747B89;">${label}</td><td align="right" style="padding:11px 0${last ? "" : ";border-bottom:1px solid #252934"};font-size:13px;line-height:18px;font-weight:700;color:#F8FAFC;">${value}</td></tr>`;
  return `<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;background:#0C0E14;border:1px solid #2A2E39;border-radius:13px;"><tr><td style="padding:20px 20px 5px;color:#F8FAFC;font-size:14px;line-height:20px;font-weight:700;">Order details</td></tr><tr><td style="padding:0 20px 10px;"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0">${row("Order ID", esc(orderId(order)))}${row("Platform", esc(order.platform))}${row("Service", esc(order.service_name))}${row("Quantity", esc(order.quantity))}${row("Amount", esc(amount(order.charge)))}${row("Status", esc(order.status))}${row("Order date", esc(orderDate(order.created_at)), true)}</table></td></tr></table>`;
}

export function signupNoOrder(name?: string | null): EmailTemplate {
  const url = `${site()}/dashboard/new-order`;
  return { subject: "Your SocialRUSH account is ready — start your first campaign", html: frame({ preheader: "Your SocialRUSH account is ready. Choose a service and launch your first campaign.", headline: `Hi ${esc(first(name))}, your next campaign starts here.`, body: "Your SocialRUSH account is set up and ready to use. Choose the service that matches your growth goal, review the details, and place your first order in a few simple steps.", card: `<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;background:#0C0E14;border:1px solid #2A2E39;border-radius:13px;"><tr><td style="padding:20px;"><p style="margin:0 0 8px;color:#F8FAFC;font-size:15px;line-height:22px;font-weight:700;">Built for a straightforward experience</p><p style="margin:0;color:#A8AFBD;font-size:14px;line-height:22px;">Explore services, see pricing before you order, and track your order status from your SocialRUSH dashboard.</p></td></tr></table>`, cta: "Start Your First Order", href: url, support: "Not sure which service is right for you? Reply to this email and we’ll help you choose the best option for your goal." }), text: `Hi ${first(name)}, your next campaign starts here.\n\nYour SocialRUSH account is set up and ready to use. Choose the service that matches your growth goal, review the details, and place your first order in a few simple steps.\n\nBuilt for a straightforward experience: explore services, see pricing before you order, and track your order status from your dashboard.\n\nStart your first order: ${url}\n\nNot sure which service is right for you? Reply to this email and we’ll help you choose the best option for your goal.` };
}

function lifecycleUnsubscribe(userId:string) { const base=site(), secret=process.env.EMAIL_UNSUBSCRIBE_SECRET; if(!secret) return `${base}/dashboard/settings`; const token=createHmac("sha256",secret).update(userId).digest("base64url"); return `${base}/api/email/unsubscribe?u=${encodeURIComponent(userId)}&t=${encodeURIComponent(token)}`; }
export function firstOrderReminder(name?:string|null,userId?:string):EmailTemplate {
  const url=`${site()}/dashboard/new-order`, unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
  const body=`Hi ${first(name)},\n\nYour SocialRUSH account is ready when you are. Choose a platform and service, enter the quantity and public link, then review your order before confirming it.\n\nPlace an order: ${url}\n\nNeed a hand? Visit ${site()}/dashboard/support\n\nYou received this because you opted in to product updates. Unsubscribe: ${unsubscribe}`;
  return {subject:"Ready to place your first SocialRUSH order?",html:frame({preheader:"A simple way to start your first SocialRUSH order.",headline:`Ready when you are, ${esc(first(name))}.`,body:"Choose a platform and service, enter the quantity and public link, then review your order before confirming it.",cta:"Start a New Order",href:url,support:`Need help? Visit <a href="${site()}/dashboard/support" style="color:#FF9A2E">Support</a>.<br><br><a href="${unsubscribe}" style="color:#A8AFBD">Unsubscribe from product updates</a>.`}),text:body};
}
export function inactive7d(name?:string|null,userId?:string):EmailTemplate {
  const url=`${site()}/dashboard/orders`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
  const body=`Hi ${first(name)},\n\nReady for your next SocialRUSH campaign? Your dashboard keeps your order history in one place, and you can start a new order whenever it suits you.\n\nView your orders: ${url}\n\nNeed help? Visit ${site()}/dashboard/support\n\nYou received this because you opted in to product updates. Unsubscribe: ${unsubscribe}`;
  return {subject:"Ready for your next SocialRUSH campaign?",html:frame({preheader:"Return to your dashboard when you are ready.",headline:`Welcome back, ${esc(first(name))}.`,body:"Your dashboard keeps your order history in one place, and you can start a new order whenever it suits you.",cta:"View My Orders",href:url,support:`Need help? Visit <a href="${site()}/dashboard/support" style="color:#FF9A2E">Support</a>.<br><br><a href="${unsubscribe}" style="color:#A8AFBD">Unsubscribe from product updates</a>.`}),text:body};
}
function lifecycleSupport(unsubscribe:string,extra="Need help?") { return `${extra} Visit <a href="${site()}/dashboard/support" style="color:#FF9A2E">Support</a>.<br><br><a href="${unsubscribe}" style="color:#A8AFBD">Unsubscribe from product updates</a>.`; }
export type FirstOrderOffer={reward:number;minimum:number};
function firstOrderOfferCard(offer?:FirstOrderOffer){
 if(!offer||!Number.isFinite(offer.reward)||!Number.isFinite(offer.minimum)||offer.reward<=0||offer.minimum<=0)return "";
 const reward=offer.reward.toLocaleString("en-IN"),minimum=offer.minimum.toLocaleString("en-IN");
 return `<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;margin-top:14px;background:#071B14;border:1px solid #1F7A55;border-radius:13px;"><tr><td style="padding:18px;color:#D1FAE5;font-size:14px;line-height:22px;"><strong style="display:block;color:#6EE7B7;font-size:15px;margin-bottom:5px;">₹${reward} first-order wallet bonus</strong>Complete your first order of ₹${minimum} or more. After that qualifying order is completed, ₹${reward} is added to your SocialRUSH wallet automatically. One bonus per eligible account.</td></tr></table>`;
}

export function firstOrderNudge2h(name?:string|null,userId?:string,offer?:FirstOrderOffer):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 const rewardText=offer?` Eligible first orders of ₹${offer.minimum.toLocaleString("en-IN")} or more also receive a ₹${offer.reward.toLocaleString("en-IN")} SocialRUSH wallet bonus after completion.`:"";
 return {subject:"Need help with your first SocialRUSH order?",html:frame({preheader:"Your SocialRUSH account is ready whenever you are.",headline:`Your account is ready, ${esc(first(name))}.`,body:`If you created your account to grow a social profile, you can place your first order in a few simple steps. Choose a platform, select a service, add your public link and review the price before confirming.${rewardText}`,card:firstOrderOfferCard(offer)||undefined,cta:"Choose My First Service",href:url,support:lifecycleSupport(unsubscribe,"Not sure which service or quantity to choose? Reply to this email and tell us your goal.")}),text:`Hi ${first(name)},\n\nYour SocialRUSH account is ready. Choose a platform, select a service, add your public link and review the price before confirming.${rewardText}\n\nStart here: ${url}\n\nNot sure what to choose? Reply to this email and tell us your goal.\n\nUnsubscribe: ${unsubscribe}`};
}

export function firstOrderTrust24h(name?:string|null,userId?:string,offer?:FirstOrderOffer):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 const steps=`<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;background:#0C0E14;border:1px solid #2A2E39;border-radius:13px;"><tr><td style="padding:20px;color:#A8AFBD;font-size:14px;line-height:23px;"><strong style="color:#F8FAFC">1.</strong> Choose your platform and service<br><strong style="color:#F8FAFC">2.</strong> Enter quantity and the correct public link<br><strong style="color:#F8FAFC">3.</strong> Review the price and confirm your order</td></tr></table>`;
 const card=steps+firstOrderOfferCard(offer);
 const rewardText=offer?` Eligible first orders of ₹${offer.minimum.toLocaleString("en-IN")} or more receive a ₹${offer.reward.toLocaleString("en-IN")} wallet bonus after completion.`:"";
 return {subject:"Your first SocialRUSH order takes just a few steps",html:frame({preheader:"Choose a service, review the details and place your first order.",headline:`A simple first order, ${esc(first(name))}.`,body:`You can see the service, quantity and price before you confirm. Once an order is placed, its status stays available in your dashboard.${rewardText}`,card,cta:"Start My First Order",href:url,support:lifecycleSupport(unsubscribe,"If anything is unclear, reply to this email before ordering and we’ll help.")}),text:`Hi ${first(name)},\n\nYour first order is simple: choose a platform and service, enter the quantity and correct public link, review the price, then confirm.${rewardText}\n\nStart your first order: ${url}\n\nNeed help? Reply to this email.\n\nUnsubscribe: ${unsubscribe}`};
}

export function firstOrderReminder3d(name?:string|null,userId?:string,offer?:FirstOrderOffer):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 const rewardText=offer?` If your first order reaches ₹${offer.minimum.toLocaleString("en-IN")}, a ₹${offer.reward.toLocaleString("en-IN")} wallet bonus is added after completion.`:"";
 return {subject:"Still deciding? Start with the service you need most",html:frame({preheader:"You can start with one service and a quantity that fits your current goal.",headline:`Start with what you need most, ${esc(first(name))}.`,body:`You don’t need to plan everything at once. Pick the platform you want to grow first, choose the service that matches your goal, and start with a quantity you’re comfortable with.${rewardText}`,card:firstOrderOfferCard(offer)||undefined,cta:"Explore Services",href:url,support:lifecycleSupport(unsubscribe,"Want a recommendation? Reply with the platform you want to grow and we’ll point you in the right direction.")}),text:`Hi ${first(name)},\n\nYou don’t need to plan everything at once. Pick the platform you want to grow first and start with a quantity you’re comfortable with.${rewardText}\n\nExplore services: ${url}\n\nReply if you want help choosing.\n\nUnsubscribe: ${unsubscribe}`};
}

export function firstOrderFinal7d(name?:string|null,userId?:string,offer?:FirstOrderOffer):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 const rewardText=offer?` Your current first-order offer is a ₹${offer.reward.toLocaleString("en-IN")} wallet bonus after completing an eligible order of ₹${offer.minimum.toLocaleString("en-IN")} or more.`:"";
 return {subject:"Your SocialRUSH account is still ready when you are",html:frame({preheader:"A final first-order check-in from SocialRUSH.",headline:`Whenever you’re ready, ${esc(first(name))}.`,body:`This is our final first-order check-in. Your account is ready, and you can return anytime to choose a service and place an order from your dashboard.${rewardText}`,card:firstOrderOfferCard(offer)||undefined,cta:"Return to New Order",href:url,support:lifecycleSupport(unsubscribe,"If something stopped you from ordering—service choice, quantity, link or payment—reply and tell us. Your feedback helps us improve.")}),text:`Hi ${first(name)},\n\nThis is our final first-order check-in. Your SocialRUSH account is ready whenever you want to place an order.${rewardText}\n\nReturn to New Order: ${url}\n\nIf something stopped you from ordering, reply and tell us.\n\nUnsubscribe: ${unsubscribe}`};
}

export function neverOrderedReactivation(name?:string|null,userId?:string,offer?:FirstOrderOffer):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 const card=firstOrderOfferCard(offer);
 const rewardText=offer?` You’re also eligible for a ₹${offer.reward.toLocaleString("en-IN")} wallet bonus after completing a first order of ₹${offer.minimum.toLocaleString("en-IN")} or more.`:"";
 return {subject:offer?`Get ₹${offer.reward.toLocaleString("en-IN")} after your first SocialRUSH order`:"Your SocialRUSH account is ready for your first order",html:frame({preheader:offer?`Complete an eligible first order and receive ₹${offer.reward.toLocaleString("en-IN")} in your wallet.`:"You already have a SocialRUSH account. Your first order can be started anytime.",headline:`Still planning your first order, ${esc(first(name))}?`,body:`You already created a SocialRUSH account, but we haven’t seen a first order yet. If you still have a social growth goal, your dashboard is ready. Choose the platform and service that fits what you need now.${rewardText}`,card:card||undefined,cta:"Go to New Order",href:url,support:lifecycleSupport(unsubscribe,"If you registered earlier but weren’t sure what to order, reply with your platform and goal. We’ll help you choose.")}),text:`Hi ${first(name)},\n\nYou already created a SocialRUSH account, but we haven’t seen a first order yet. If you still have a social growth goal, your dashboard is ready.${rewardText}\n\nGo to New Order: ${url}\n\nReply with your platform and goal if you want help choosing.\n\nUnsubscribe: ${unsubscribe}`};
}
export function abandonedOrderReminder(name:string|null|undefined,userId:string|undefined,draft:{platform:string;serviceName:string;quantity:number},offer?:FirstOrderOffer):EmailTemplate {
 const url=`${site()}/dashboard/new-order?draft=1`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 const platform=esc(draft.platform), service=esc(draft.serviceName), quantity=Number(draft.quantity||0).toLocaleString("en-IN");
 const savedCard=`<table role="presentation" width="100%" cellspacing="0" cellpadding="0" border="0" style="width:100%;background:#0C0E14;border:1px solid #2A2E39;border-radius:13px;"><tr><td style="padding:20px;"><p style="margin:0 0 8px;color:#F8FAFC;font-size:15px;line-height:22px;font-weight:700;">Your saved order</p><p style="margin:0;color:#A8AFBD;font-size:14px;line-height:22px;">${platform} · ${service}<br>Quantity: ${quantity}</p></td></tr></table>`;
 const card=savedCard+firstOrderOfferCard(offer);
 return {subject:"Your SocialRUSH order is saved — continue when ready",html:frame({preheader:"Your unfinished SocialRUSH order is saved in your dashboard.",headline:`Your order is still saved, ${esc(first(name))}.`,body:"You started configuring an order but didn’t finish checkout. Your saved configuration is still available, so you can return, review the current price and continue when you’re ready.",card,cta:"Continue My Saved Order",href:url,support:lifecycleSupport(unsubscribe,"If something stopped you—service choice, quantity, link or payment—reply to this email and we’ll help.")}),text:`Hi ${first(name)},\n\nYou started configuring a SocialRUSH order but didn’t finish checkout.\n\nSaved order: ${draft.platform} · ${draft.serviceName} · Quantity ${quantity}\n\nContinue your saved order: ${url}\n\nIf something stopped you, reply and tell us what happened.\n\nUnsubscribe: ${unsubscribe}`};
}

export function inactivePremium(name:string|null|undefined,userId:string|undefined,kind:"vip"|"high_value"):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`, valued=kind==="vip"?"valued":"high-value";
 const body=`Hi ${first(name)},\n\nIt’s great to have you as one of our ${valued} SocialRUSH customers. Whenever you’re planning your next campaign, your account, previous orders and services are available from your dashboard, making it easy to pick up where you left off.\n\nPlan my next campaign: ${url}\n\nIf you have a specific growth target or need help deciding which service fits your next campaign, reply directly to this email.\n\nUnsubscribe: ${unsubscribe}`;
 return {subject:"Ready for your next SocialRUSH campaign?",html:frame({preheader:"Your next SocialRUSH campaign is ready when you are.",headline:`Ready for your next campaign, ${esc(first(name))}?`,body:`It’s great to have you as one of our ${valued} SocialRUSH customers. Your account, previous orders and services are ready whenever you are.`,cta:"Plan My Next Campaign",href:url,support:lifecycleSupport(unsubscribe,"Have a specific growth target or need help choosing a service? Reply directly to this email.")}),text:body};
}
export function inactivePlatform(name:string|null|undefined,userId:string|undefined,platform:string):EmailTemplate {
 const url=`${site()}/dashboard/new-order`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`,firstName=first(name);
 const body=`Hi ${firstName},\n\nYou’ve previously used SocialRUSH for ${platform}, so whenever you’re planning your next campaign, your account is ready to go.\n\nContinue growing on ${platform}: ${url}\n\nNeed help? Visit ${site()}/dashboard/support\n\nUnsubscribe: ${unsubscribe}`;
 return {subject:`Ready for your next ${platform} campaign?`,html:frame({preheader:`Continue your ${platform} campaign when you are ready.`,headline:`Ready to continue growing on ${esc(platform)}, ${esc(firstName)}?`,body:`You’ve previously used SocialRUSH for ${esc(platform)}, so whenever you’re planning your next campaign, your account is ready to go.`,cta:`Continue Growing on ${platform}`,href:url,support:lifecycleSupport(unsubscribe)}),text:body};
}
export function inactiveLongTerm(name?:string|null,userId?:string):EmailTemplate {
 const url=`${site()}/dashboard/orders`,unsubscribe=userId?lifecycleUnsubscribe(userId):`${site()}/dashboard/settings`;
 return {subject:"It’s been a while — ready for your next campaign?",html:frame({preheader:"Return whenever your next campaign feels right.",headline:`Welcome back, ${esc(first(name))}.`,body:"It’s been a while, and we’ll be here whenever you’re ready to plan your next campaign. Your SocialRUSH dashboard keeps your account and order history close at hand.",cta:"Return to My SocialRUSH Dashboard",href:url,support:lifecycleSupport(unsubscribe)}),text:`Hi ${first(name)},\n\nIt’s been a while, and we’ll be here whenever you’re ready to plan your next campaign.\n\nReturn to your dashboard: ${url}\n\nUnsubscribe: ${unsubscribe}`};
}

export function orderCreated(name: string | null | undefined, order: Order): EmailTemplate {
  const url = `${site()}/dashboard/orders/${order.id}`;
  return { subject: `Order received — SocialRUSH ${orderId(order)}`, html: frame({ preheader: `Your SocialRUSH order ${esc(orderId(order))} has been received.`, headline: `Thanks, ${esc(first(name))}. Your order is confirmed.`, body: "We’ve successfully received your order and it is now in our system. You can review the order details and follow its progress from your dashboard.", card: details(order), cta: "Track Your Order", href: url, support: `If anything looks incorrect or you need assistance, reply to this email and include your order ID (${esc(orderId(order))}) so our team can help quickly.` }), text: `Thanks, ${first(name)}. Your order is confirmed.\n\nWe’ve successfully received your order and it is now in our system.\n\nOrder ID: ${orderId(order)}\nPlatform: ${order.platform || "—"}\nService: ${order.service_name || "—"}\nQuantity: ${order.quantity ?? "—"}\nAmount: ${amount(order.charge)}\nStatus: ${order.status || "—"}\nOrder date: ${orderDate(order.created_at)}\n\nTrack your order: ${url}\n\nIf anything looks incorrect or you need assistance, reply to this email and include your order ID so our team can help quickly.` };
}

export function orderCompleted(name: string | null | undefined, order: Order): EmailTemplate {
  const url = `${site()}/dashboard/orders`;
  return { subject: `Order completed — SocialRUSH ${orderId(order)}`, html: frame({ preheader: `Your SocialRUSH order ${esc(orderId(order))} is complete.`, headline: `Your order is complete, ${esc(first(name))}.`, body: "Your SocialRUSH order has been marked as completed. Thank you for choosing us to support your social media growth.", card: details(order), cta: "View My Orders", href: url, support: "Have a question about this order or need help with your next campaign? Reply to this email and our team will be happy to assist." }), text: `Your order is complete, ${first(name)}.\n\nYour SocialRUSH order has been marked as completed. Thank you for choosing us to support your social media growth.\n\nOrder ID: ${orderId(order)}\nPlatform: ${order.platform || "—"}\nService: ${order.service_name || "—"}\nQuantity: ${order.quantity ?? "—"}\nAmount: ${amount(order.charge)}\nStatus: ${order.status || "—"}\nOrder date: ${orderDate(order.created_at)}\n\nView your orders: ${url}\n\nHave a question about this order or need help with your next campaign? Reply to this email and our team will be happy to assist.` };
}
