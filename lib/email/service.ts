import "server-only";
import { createAdminClient } from "@/lib/supabase/admin";
import { orderCompleted, orderCreated, signupNoOrder, type EmailTemplate } from "@/lib/email/templates";
type Event = { id:string; user_id:string; order_id:string|null; event_type:"signup_no_order"|"order_created"|"order_completed"; recipient:string };
function safeError(error: unknown) { return error instanceof Error ? error.message.slice(0, 500) : "Email provider request failed"; }
async function deliver(recipient: string, message: EmailTemplate) { const key=process.env.RESEND_API_KEY, from=process.env.EMAIL_FROM, reply=process.env.REPLY_TO_EMAIL; if (!key || !from || !reply) throw new Error("Email delivery is not configured"); const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[recipient],reply_to:reply,subject:message.subject,html:message.html,text:message.text})}); const payload=await response.json().catch(()=>null) as {id?:string;message?:string}|null; if(!response.ok) throw new Error(`Resend request failed (${response.status}): ${payload?.message || "unknown"}`); return payload?.id || null; }
export async function processCustomerEmailEvents(limit=20) { const db=createAdminClient(); let processed=0; const {error:enqueueError}=await db.rpc("enqueue_signup_no_order_email_events"); if(enqueueError) throw enqueueError; for(let i=0;i<limit;i++){ const {data,error}=await db.rpc("claim_customer_email_event"); if(error) throw error; const event=(data?.[0] || null) as Event|null; if(!event) break; processed++; try { const {data:profile}=await db.from("profiles").select("full_name").eq("id",event.user_id).single(); if(event.event_type==="signup_no_order"){const {count}=await db.from("orders").select("id",{count:"exact",head:true}).eq("user_id",event.user_id); const {data:prefs}=await db.from("profiles").select("notification_preferences").eq("id",event.user_id).single(); if((count||0)>0 || prefs?.notification_preferences?.marketing===false){ await db.from("customer_email_events").update({status:"sent",sent_at:new Date().toISOString(),error_message:count?"Skipped: user already has an order":"Skipped: marketing opt-out"}).eq("id",event.id); console.info("[email] signup follow-up skipped",{eventId:event.id,reason:count?"has_order":"marketing_opt_out"}); continue; }} let template:EmailTemplate; if(event.event_type==="signup_no_order") template=signupNoOrder(profile?.full_name); else { const {data:order,error:orderError}=await db.from("orders").select("id,public_order_id,platform,service_name,quantity,charge,status,created_at").eq("id",event.order_id!).single(); if(orderError || !order) throw new Error("Order details are unavailable"); template=event.event_type==="order_created"?orderCreated(profile?.full_name,order):orderCompleted(profile?.full_name,order); } const id=await deliver(event.recipient,template); await db.from("customer_email_events").update({status:"sent",provider_message_id:id,sent_at:new Date().toISOString(),processing_started_at:null}).eq("id",event.id); console.info("[email] sent",{eventId:event.id,eventType:event.event_type,orderId:event.order_id,providerMessageId:id}); } catch(error) { const message=safeError(error); await db.from("customer_email_events").update({status:"failed",error_message:message,processing_started_at:null}).eq("id",event.id); console.error("[email] provider failure",{eventId:event.id,eventType:event.event_type,orderId:event.order_id,error:message}); }} return processed; }
export async function sendEmailTest() { return deliver("rushalthakur240@gmail.com",signupNoOrder("Rushal")); }
